The job failed because the TypeScript compiler could not find the module "@alice/shared/schema" or its type declarations when building /app/packages/database. The error occurred in src/index.ts, line 4:

```
src/index.ts(4,25): error TS2307: Cannot find module '@alice/shared/schema' or its corresponding type declarations.
```

## Solution

1. **Check if @alice/shared/schema exists:** Ensure that the module "@alice/shared/schema" is present in your monorepo, typically under `packages/shared/schema` or similar.

2. **Make sure it's built before @alice/database:** If "@alice/shared/schema" is another internal package, confirm it gets built or its type declarations are generated before "@alice/database" is built. In your workspace, this often involves setting up proper dependencies in `package.json`:

```json
// packages/database/package.json
"dependencies": {
  "@alice/shared/schema": "*"
}
```

3. **Check tsconfig paths:** If you’re using path aliases in your `tsconfig.json`, ensure the compiler can resolve the module:

```json
// packages/database/tsconfig.json
{
  "compilerOptions": {
    "baseUrl": "..",
    "paths": {
      "@alice/shared/schema": ["../shared/schema/src"]
    }
  }
}
```

4. **Install and build @alice/shared/schema:** Run pnpm install and ensure pnpm builds all workspace packages in the correct order:

```bash
pnpm install
pnpm build
```

5. **Export types/index.ts:** Ensure "@alice/shared/schema" exports its types, so TypeScript picks them up.

## Code Suggestions

If the package or files are missing, create them as needed:

```typescript
// packages/shared/schema/src/index.ts
export interface MySchema { /* ... */ }
// or export type declarations
```

If you need to reference a folder, update import statements accordingly:

```typescript
// packages/database/src/index.ts
import { MySchema } from '@alice/shared/schema';
```

## Summary

- Add or restore "@alice/shared/schema" package.
- Ensure it is included as a dependency and gets built first.
- Check TypeScript path resolution.
- Run a clean install and build.

After applying these fixes, re-run the workflow. Let me know if you need help with any of these steps!
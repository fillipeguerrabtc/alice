The failing job in your workflow was caused by the error:

```
src/index.ts(4,25): error TS2307: Cannot find module '@alice/shared' or its corresponding type declarations.
```

**Root Cause:**  
Your `packages/database/src/index.ts` file imports `* as schema from '@alice/shared';`, but TypeScript cannot resolve the `@alice/shared` module. This usually indicates one of these issues:
- The `@alice/shared` package is missing from your `node_modules` or not properly linked.
- The `@alice/shared` package does not have its type declarations built.
- Workspace references are misconfigured in your monorepo (e.g., missing in tsconfig or package.json).

**How to Fix:**
1. Ensure `@alice/shared` is properly referenced in your workspace:
   - In the root/package.json, include `"@alice/shared"` as a workspace.
   - In `packages/database/package.json`, add `"@alice/shared"` to `dependencies`.

2. Build or link the shared package before building database:
   - If your monorepo uses TypeScript project references, include `@alice/shared` in `packages/database/tsconfig.json` under `references`.
   - Ensure `pnpm build` or your build command builds `@alice/shared` before it builds `@alice/database`.

   Example (partial) for `tsconfig.json`:
   ```json
   {
     "compilerOptions": { "composite": true, ... },
     "references": [
       { "path": "../shared" }
     ]
   }
   ```

   Example for `package.json` dependencies:
   ```json
   {
     "dependencies": {
       "@alice/shared": "workspace:*"
     }
   }
   ```

3. If you use a custom path for module import, check your `tsconfig.json`:
   ```json
   {
     "compilerOptions": {
       "baseUrl": "./",
       "paths": {
         "@alice/shared": ["../shared/src"]
       }
     }
   }
   ```

**Summary of Required Actions:**
- Add and build the `@alice/shared` package before `@alice/database`.
- Verify correct TypeScript and workspace configuration.
- Make sure all type declarations for `@alice/shared` are generated and available.

After making these changes, re-run your workflow. This will resolve the missing module issue and allow your build to complete successfully.

For reference, review your [packages/database/src/index.ts](https://github.com/fillipeguerrabtc/alice/blob/01ad911e3d3116c089b66afc5d1c5b944d047932/packages/database/src/index.ts) (branch/commit `01ad911e3d3116c089b66afc5d1c5b944d047932`) to confirm the import path and context.
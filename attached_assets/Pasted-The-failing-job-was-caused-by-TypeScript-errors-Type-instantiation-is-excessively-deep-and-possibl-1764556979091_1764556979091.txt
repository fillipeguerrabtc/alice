The failing job was caused by TypeScript errors: "Type instantiation is excessively deep and possibly infinite" in several files, including:
- apps/frontend-service/src/pages/Agents.tsx (line 138)
- apps/frontend-service/src/pages/ModulesAdmin.tsx (line 293)
- apps/frontend-service/src/pages/Namespaces.tsx (line 118)
- client/src/pages/Agents.tsx (line 138)
- client/src/pages/ModulesAdmin.tsx (line 293)
- client/src/pages/Namespaces.tsx (line 118)

## Solution Steps

### 1. Fix Excessive Type Instantiation

This error typically happens with deeply nested or recursive TypeScript types, often in generic-heavy code or when working with complex forms or data structures. Solutions:

- **Refactor the type** so it isn’t recursive or so deeply nested.
- Use the `any` or `unknown` type temporarily to break infinite recursion (not recommended for production, but useful for diagnosis).
- Consider splitting large union types or interfaces.
- Use helper utility types to simplify type logic.

### 2. Code Suggestions

Find the complex type or generic at the reported line.

For example, if the type is something like:
```ts
type DeepType<T> = { a: DeepType<T> };
```
Replace with:
```ts
type DeepType<T> = { a?: T }; // or { a?: any }
```
Or, use a maximum depth guard:
```ts
type DeepType<T, Depth extends number = 10> = Depth extends 0 ? T : { a: DeepType<T, Prev<Depth>> };
```
If you’re using `zod`, `react-hook-form`, or similar libraries, simplify schemas or type inference.

### 3. Short-Term Bypass

Add this to your `tsconfig.json` for easier debugging:
```json
{
  "compilerOptions": {
    "skipLibCheck": true
  }
}
```

### 4. Verify Generation

Make sure your build script is producing all necessary output files:
- Confirm `packages/$pkg/dist/index.js` exists after the build.
- Address any other build errors before rerunning tests.

---

**Summary**: Refactor the type definitions at the mentioned lines by reducing type complexity, limiting recursive generics, or temporarily relaxing types. Once the instantiation problem is resolved, rerun your build to confirm the fix.

If you provide the code from those specific lines, I can suggest direct type changes.
Your job is failing due to TypeScript errors related to propertiesThe job is failing due to TypeScript errors in your codebase that are missing from your user data types. Specifically, properties like googleId, githubId, microsoft—specifically, you’re referencing properties ('googleId, samlNameId, and emailVerified are referenced inId', 'githubId', 'microsoftId', 'samlNameId', ' apps/auth-service/src/index.ts, but they do not existemailVerified') on the users table or related user objects in the declared type for your user object.

** that do not exist in their type definitions. YouCommon error pattern:**
```
error TS2339: also have a type mismatch in packages/shared-utils/src/r Property 'googleId' does not exist on type ...
Object literal maybac/types.ts about the property 'user'.

Here’s only specify known properties, and 'googleId' does not exist in type ...
Subsequent property declarations must how you can fix the root issues:

### 1. Missing Properties in Users Table/Object

Either:
- Add the required properties to the users table definition (database have the same type...
```

## Solution

1. **Update your user type/schema definition:**
   - migration & TypeScript types).
- Or, remove/rewrite code Locate the file where the user type is defined for your database table (often something like packages/shared-utils referencing these properties if they are not meant to be part of your schema.

#### Example fix for Type/src/rbac/types.ts or any model/schema file forScript types (e.g., for googleId):
Update your user "users").
   - Add the missing properties (googleId, model definition, e.g.:
```typescript
// Somewhere in githubId, microsoftId, samlNameId, email your DB schema or type definition
export type User =Verified, etc.) with appropriate types.

**Example:**
 {
  id: string;
  /* ...other fields... */
  googleId?: string;
  githubId?: string;
  microsoftId?:```ts
// Extend your user type definition to include the missing properties
export type User = {
  id: string;
  ativo: boolean | null;
  tenantId: string | null;
  email: string | null;
  firstName: string | null;
  lastName: string | null;
  profileImageUrl: string | null;
  // Add these:
  googleId?: string | null;
  githubId?: string | null;
  microsoftId?: string | null;
  samlNameId?: string | null;
  emailVerified?: boolean | null; // or string if you store it differently
  // ...any other properties referenced in index string;
  samlNameId?: string;
  emailVerified?: boolean;
  // Add other missing fields if needed
}
```
Or, if you’re using a library for DB schema (e.g., drizzle), ensure you add columns for these fields.

#### Or, update your queries to avoid referencing these properties if not needed:
```typescript
// Remove properties from object literals that don't exist on the model
const user = {
  id: '...',
  // googleId: '...', // <-- Remove this line if not part of your schema
  // same for githubId, etc.
}
```

### 2. RBAC Types File Type Mismatch

You have this error:
> packages/shared-utils/src/rbac/types.ts(153,7): error TS2717: Subsequent property declarations must have the same type. Property 'user' must be of type 'User | undefined', but here has type 'AuthContext | undefined'.

You need to ensure that all declarations of the 'user' property have a consistent type. For example:
```typescript
// Instead of mixing User | undefined and AuthContext | undefined, pick one:
user?: User;
```
Or refactor your types so that 'user' is always the same type everywhere.

---

### Next Steps (Recommended)

- Review and update the type/schema definitions for your users table and related objects:
  - If those IDs and emailVerified are valid columns, add them to your DB migration and update corresponding TypeScript types.
  - If not, remove them from your code wherever referenced.
- Fix the RBAC types mismatch by ensuring 'user' property type is consistent.

Check all locations mentioned in the errors:
- [apps/auth-service/src/index.ts](https://github.com/fillipeguerrabtc/alice/blob/856e0e2bbf81b1f4e7a776ee19b96aa62916f58f/apps/auth-service/src/index.ts)
- [packages/shared-utils/src/rbac/types.ts](https://github.com/fillipeguerrabtc/alice/blob/856e0e2bbf81b1f4e7a776ee19b96aa62916f58f/packages/shared-utils/src/rbac/types.ts)

Make the schema and types match, and your build should pass! If you need exact code snippets for any specific file/line, just let me know.
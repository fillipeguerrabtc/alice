The failing job’s log shows this error:

src/index.ts(4,25): error TS2307: Cannot find module '@alice/shared' or its corresponding type declarations.

## Cause
Your build is failing because TypeScript can't resolve the `@alice/shared` module in `packages/database/src/index.ts`. This is often due to missing dependencies, an incorrect path/alias, or the module not being built/published.

## Solutions

### 1. Check Module Availability
- Ensure that the package or folder `packages/shared` exists.
- If using TypeScript with project references, make sure `@alice/shared` is built before `@alice/database`. 
- If using workspace tools (like pnpm workspaces), ensure that `@alice/shared` is listed properly in the dependencies of `@alice/database`.

#### package.json of database should include:
```json
"dependencies": {
  "@alice/shared": "*"
}
```

### 2. Check Path Configuration
- If you're using path aliases, ensure `tsconfig.json` in `packages/database` includes correct paths:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@alice/shared": ["../shared/src"]
    }
  }
}
```
- Verify that `packages/shared` is built and has an `index.d.ts` (or equivalent type declaration).


### 3. Build Shared Package First
If using TypeScript `tsc -b` with project references, add a `tsconfig.json` in the root:
```json
{
  "files": [],
  "references": [
    { "path": "packages/shared" },
    { "path": "packages/database" }
  ]
}
```

Then run:
```sh
tsc -b
```
This ensures packages are built in proper order.

### 4. Install Workspace Dependencies
Run:
```sh
pnpm install
```
Or, if using npm/yarn workspaces:
```sh
npm install
```
This links local packages.

---

## Summary
- Ensure `@alice/shared` exists and is built.
- Check your `tsconfig.json` for correct path alias configuration.
- Make sure dependencies are installed and workspace links are up to date.
- Add package references so TypeScript builds shared first.

Would you like help reviewing or updating any specific configuration file?
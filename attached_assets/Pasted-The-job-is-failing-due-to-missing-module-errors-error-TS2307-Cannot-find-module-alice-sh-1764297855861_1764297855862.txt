The job is failing due to missing module errors:

```
error TS2307: Cannot find module '@alice/shared-utils' or its corresponding type declarations.
```

This affects multiple files:
- apps/auth-service/src/index.ts (line 35)
- apps/chat-service/src/index.ts (line 27)
- apps/integrations-service/src/index.ts (line 20)
- apps/rag-service/src/index.ts (line 27)
- apps/training-service/src/index.ts (line 25)

## Solution

The error means that TypeScript cannot find the `@alice/shared-utils` package. Here are actionable steps to fix it:

### 1. Ensure Dependency Is Installed

If `@alice/shared-utils` is a local package, make sure it's present in the repository (usually under `packages/shared-utils` or similar).

- Add its path to your root `tsconfig.json` or any relevant `tsconfig` using `paths` mapping:

```jsonc
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@alice/shared-utils": ["packages/shared-utils/src"]
    }
  }
}
```

- Run:
  ```bash
  npm install
  npm run build
  ```

### 2. Check `package.json` References

Make sure all relevant `package.json` files include the dependency:

```json
"dependencies": {
  "@alice/shared-utils": "*"
}
```

If it’s a local workspace package, ensure your workspace is set up using either Yarn workspaces or npm workspaces.

### 3. Build Shared Utils First

If you have a monorepo, the shared package needs to be built before it’s consumed. Update your workflows to build `@alice/shared-utils` as a prerequisite step.

### 4. Type Declarations

If `@alice/shared-utils` lacks `.d.ts` files, generate them by adding to its `tsconfig.json`:

```json
"declaration": true
```
Then run:

```bash
tsc -p packages/shared-utils/tsconfig.json
```

---

## Suggested Workflow Update (partial)

Add before typecheck:

```yaml
- name: Build shared utils
  run: cd packages/shared-utils && npm install && npm run build
```

---

## Summary

- Ensure `@alice/shared-utils` exists and is properly built
- Update `tsconfig.json` for path mapping
- Reference it in `package.json` dependencies
- Adjust workflow to build it first

Resolve these, and the job should pass typecheck. Let me know if you need help with your specific project structure or want links to any config or source files for confirmation.
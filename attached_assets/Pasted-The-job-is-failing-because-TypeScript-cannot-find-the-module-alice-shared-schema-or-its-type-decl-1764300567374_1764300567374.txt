The job is failing because TypeScript cannot find the module '@alice/shared/schema' or its type declarations. This error appears at src/index.ts line 4 in the database package:

```
src/index.ts(4,25): error TS2307: Cannot find module '@alice/shared/schema' or its corresponding type declarations.
```

## Solution Steps

1. **Check for missing package**
   - Make sure that `@alice/shared` is listed as a dependency in `packages/database/package.json`.
   - Example fix in `package.json`:
     ```json
     "dependencies": {
       "@alice/shared": "workspace:*"
     }
     ```

2. **Install and build workspace packages**
   - Double-check if the `@alice/shared` package exists inside the `packages/shared` directory.
   - Run the following from the repository root to ensure all packages are installed and built:
     ```bash
     pnpm install
     pnpm run build
     ```
   - If using workspaces, ensure that the workspace configuration includes `packages/shared`.

3. **Check type declarations**
   - If the module exists, check that its `schema` export is present and that TypeScript type declarations are generated (usually in a `dist` or `types` directory).
   - Add an `index.d.ts` file or ensure that your build step generates types for the shared package.

4. **Relative vs. Package Import**
   - If `@alice/shared/schema` is intended as a relative import, use a relative path, e.g.:
     ```ts
     import { ... } from '../../shared/src/schema';
     ```

## Code Suggestions

- In `packages/database/package.json`:
  ```json
  {
    "dependencies": {
      "@alice/shared": "workspace:*"
    }
  }
  ```
- Check `packages/shared/src/schema.ts` exists and is exported in `packages/shared/src/index.ts`.

- In `packages/shared/package.json`, ensure:
  ```json
  {
    "main": "dist/index.js",
    "types": "dist/index.d.ts"
  }
  ```
- Rebuild everything:
  ```bash
  pnpm install
  pnpm run build
  ```

---

Once these changes are made, the build should be able to resolve `@alice/shared/schema` and proceed without this error. If you need direct code edits or further troubleshooting, please provide the relevant files.
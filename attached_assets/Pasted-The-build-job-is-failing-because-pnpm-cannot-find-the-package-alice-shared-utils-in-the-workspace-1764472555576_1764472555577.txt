The build job is failing because pnpm cannot find the package "@alice/shared-utils" in the workspace, but it's listed as a dependency (with "@alice/shared-utils@workspace:*"). The error message is:

ERR_PNPM_WORKSPACE_PKG_NOT_FOUND  In : "@alice/shared-utils@workspace:*" is in the dependencies but no package named "@alice/shared-utils" is present in the workspace

## Solution Steps

1. **Check for the package directory**:
   - Confirm there’s a directory/package called `shared-utils` under the workspace root (commonly under `packages/` or similar).
   - Example:
     ```
     /packages
       /shared-utils
         package.json
     ```

2. **If the package is missing, create it or fix the dependency**:
   - If `@alice/shared-utils` should exist, add it to your monorepo:
     - Create a directory (e.g., `packages/shared-utils`)
     - Add a `package.json` with the correct name:
       ```json
       {
         "name": "@alice/shared-utils",
         "version": "1.0.0",
         "main": "index.js"
       }
       ```
   - If it’s a mistake (should not be a dependency), remove or replace `@alice/shared-utils@workspace:*` from your dependencies in `/app/package.json` (or the affected package).

3. **Update workspace configuration**:
   - Confirm your workspace settings in `pnpm-workspace.yaml` include all relevant packages:
     ```yaml
     packages:
       - 'packages/*'
       - 'other-folder/*'
     ```

4. **Install again**:
   - Commit changes, push, and re-run the build.
   - Run locally if possible: `pnpm install` and ensure there are no workspace errors.

## Example Fix if Dependency Should be Removed
Remove `"@alice/shared-utils": "workspace:*"` from your `package.json` dependencies and then run:

```sh
pnpm install
```

## Example Fix if Package Should Be Added

Create `packages/shared-utils/package.json`:
```json
{
  "name": "@alice/shared-utils",
  "version": "1.0.0",
  "main": "index.js"
}
```
And add a basic `index.js` if needed.

## Summary

- Fix: Ensure `@alice/shared-utils` exists as a workspace package, or remove it from dependencies.
- Cause: pnpm cannot find the referenced workspace package.
- Next step: Update your repo, rerun the workflow.

Let me know if you want help with specific code edits or want to see the relevant `package.json` file content.
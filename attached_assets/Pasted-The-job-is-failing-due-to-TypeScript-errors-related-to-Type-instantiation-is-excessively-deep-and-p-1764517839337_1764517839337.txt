The job is failing due to TypeScript errors related to "Type instantiation is excessively deep and possibly infinite" at the following locations:

- apps/frontend-service/src/pages/Agents.tsx(131,15)
- apps/frontend-service/src/pages/ModulesAdmin.tsx(286,15)
- apps/frontend-service/src/pages/Namespaces.tsx(111,15)
- client/src/pages/Agents.tsx(131,15)
- client/src/pages/ModulesAdmin.tsx(286,15)
- client/src/pages/Namespaces.tsx(111,15)

## Solution Steps

1. **Identify Recursive or Deeply Nested Types**
   TypeScript throws this error when types or generics recursively reference themselves or get nested too deeply.

2. **Review the Problematic Lines**
   Examine the type at the indicated line numbers (e.g., line 131 in Agents.tsx). Look for complex types, chained generics, or recursive type definitions. 

3. **Simplify or Refactor Types**
   - Limit recursion by capping the depth with a generic parameter (e.g., add a `Depth` parameter).
   - Break up large, nested type structures.
   - In some cases, reduce the complexity of utility types (mapped, conditional, etc.).
   - Instead of inferred deeply-nested objects, explicitly declare types when possible.

### Example Solution
**If you have a type or function like this:**
```ts
type DeepType<T> = { value: T, next: DeepType<T> }; // Recursive without end
```
**Refactor with a depth limit:**
```ts
type DeepType<T, Depth extends number = 10> = Depth extends 0 ? { value: T } : { value: T, next: DeepType<T, Decrement<Depth>> };
// Where Decrement is a utility type reducing Depth
```
**Or, simplify the type entirely, breaking recursion:**
```ts
type SimpleType<T> = { value: T, next?: SimpleType<T> };
```

## What To Do Next

- Open the referenced files at the specified lines:
  - [apps/frontend-service/src/pages/Agents.tsx (line 131)](https://github.com/fillipeguerrabtc/alice/blob/1a197f536623836eef4030a2cb1968dbcb01b9f2/apps/frontend-service/src/pages/Agents.tsx#L131)
  - [apps/frontend-service/src/pages/ModulesAdmin.tsx (line 286)](https://github.com/fillipeguerrabtc/alice/blob/1a197f536623836eef4030a2cb1968dbcb01b9f2/apps/frontend-service/src/pages/ModulesAdmin.tsx#L286)
  - [apps/frontend-service/src/pages/Namespaces.tsx (line 111)](https://github.com/fillipeguerrabtc/alice/blob/1a197f536623836eef4030a2cb1968dbcb01b9f2/apps/frontend-service/src/pages/Namespaces.tsx#L111)
  - Repeat for the client app if relevant.

- Review types on those lines, simplify as described, and verify with a build.

**If you paste the problematic type definitions here, I can assist with concrete code suggestions!**

## Summary
- The infinite/deep type error is due to complex or recursive TypeScript types.
- Simplify or cap recursive type definitions.
- Review the types at the failing line numbers and refactor as needed.

Let me know if you'd like the specific code from those lines reviewed here for direct fixes.
# Índice de Documentação (SSOT) - Alice Enterprise Platform

**Autor:** Fillipe Guerra  
**Data:** 10 de Março de 2026  
**Objetivo:** Centralizar a fonte de verdade (SSOT) e reduzir redundância entre documentos.

---

## SSOT por domínio

| Documento | Propósito | Fonte de verdade | Observações |
|-----------|-----------|------------------|------------|
| `README.md` | Visão geral + onboarding rápido | Sim (resumo) | Deve ser conciso e apontar para docs detalhadas |
| `docs/ARQUITETURA.md` | Arquitetura (arc42 + C4 + ADRs) | Sim | Decisões e diagramas de sistema |
| `docs/ARQUITETURA-GPU-MANAGER.md` | Arquitetura GPU (Gate 2) | Sim | VRAM budget, serviços GPU, OpenAI Vision/Images |
| `docs/DEPLOYMENT.md` | Deploy e CI/CD | Sim | Workflow, ordem de deploy, checklist e troubleshooting |
| `docs/STATUS-REAL-ATUAL.md` | Estado atual (snapshot factual) | Sim | Referência operacional consolidada |
| `docs/OBSERVABILITY.md` | Observabilidade (métricas/dashboards/alertas) | Sim | Inclui auditoria consolidada |
| `docs/SISTEMA-APRENDIZADO.md` | Aprendizado contínuo (auto-learning) | Sim | Fluxos e status do sistema de aprendizado |
| `docs/TRAINING.md` | Treinamento/QLoRA | Sim | API, schedule, requisitos e fluxo |
| `docs/TREINAMENTO-LIMITES-E-BOAS-PRATICAS.md` | Limites e boas práticas de treinamento | Sim | Janelas conversa/documento, minChars, validação dimensões, resolução escopo |
| `docs/GUIA-TREINAMENTO-AGENTES.md` | Guia de treinamento para negócios | Sim | Passo a passo didático (RAG + treinamento) |
| `docs/SECRETS.md` | Secrets e variáveis | Sim | SSOT de secrets + geração de `.env.prod` |
| `docs/PERMISSIONS.md` | SSOT de permissões | Sim | UIDs/GIDs + RBAC de aplicação |
| `docs/GUIA-CONFIGURACAO-INICIAL.md` | Configuração inicial (passo a passo) | Sim | Manual completo de onboarding |
| `docs/ROADMAP.md` | Funcionalidades futuras (Trading, integrações) | Sim | Itens de expansão, não pendências |

---

## Consolidações realizadas

- **Correção de deploy em permissões (10/03/2026):** `infra/scripts/fix-production-permissions.sh` foi atualizado para lidar com corrida de arquivos efêmeros do ClickHouse durante correção recursiva de ownership (`find -exec chown`), sem promover erro transitório (`No such file or directory`) para falha fatal.
- **Correção de sintaxe de `find` no deploy (10/03/2026):** `infra/scripts/fix-production-permissions.sh` foi ajustado para montar `find` com path antes de `-ignore_readdir_race`, eliminando a falha `find: paths must precede expression` no step de preparação de deploy.
- **Hardening pós-plano concluído (10/03/2026):** cadeia de dependências produtivas foi endurecida no SSOT de config (`package.json` + `pnpm-lock.yaml`) com `pnpm.overrides` para `minimatch`, `underscore`, `lodash`, `qs` e `undici`; auditoria produtiva (`pnpm audit --prod --audit-level high`) em status limpo.
- **Idempotência mandatory por default no Training (10/03/2026):** `TRAINING_RUN_START_REQUIRE_IDEMPOTENCY_KEY` foi atualizado para `true` por default em `apps/training-service/src/index.ts`, reforçando fail-closed no start de runs.
- **Redução incremental de monólitos críticos (10/03/2026):** extrações para `apps/chat-service/src/runtime-config.ts` e `apps/training-service/src/training-job-stream.ts` reduziram densidade dos respectivos `index.ts` sem alterar contratos de API/RBAC.
- **Consolidação histórica e governança contínua (10/03/2026):** rebase/squash não-interativo concluído em base `origin/main` (backup em `backup/pre-squash-20260310-1`), com enforcement contínuo de guardrails via `package.json` (`verify:enterprise-focus`, `verify:enterprise-focus:full`, `validate:enterprise`) e baseline pós-consolidação em status OK para janelas de 50 e 418 commits.
- **Consolidação técnica adicional em Wise/Trading/Chat (10/03/2026):** `wise-payments` reduzido para 176 arquivos TS/TSX e 13.976 linhas, arquivos `<40` linhas reduzidos para 16, `TradingContent.tsx` reduzido para 1321 linhas e `useChatPageLayoutController.ts` para 591 linhas.
- **Correção dos 4 itens finais de review (10/03/2026):** WisePayments teve consolidação de wrappers redundantes em `apps/frontend-service/src/pages/wise-payments/use-wise-page-composition.ts` com redução de 193 para 187 arquivos TS/TSX; `TradingContent.tsx` e `useChatPageLayoutController.ts` tiveram redução incremental de densidade; e foi adicionado o guardrail operacional `scripts/verify-enterprise-focus.sh` para controlar churn documental, foco por domínio e fragmentação/densidade de frontend.
- **Fechamento formal do plano enterprise em 100% (10/03/2026):** status de execução consolidado como concluído em `docs/PLANO-IMPLEMENTACAO-ENTERPRISE-BLOCOS.md` após fechamento de residuais críticos de frontend e validação sequencial final por componente alterado.
- **Cleanup final de residual em Trading/Chat (10/03/2026):** `apps/frontend-service/src/pages/TradingContent.tsx` recebeu composição explícita por domínio para `section-props` e `apps/frontend-service/src/pages/Chat/useChatPageLayoutController.ts` recebeu normalização final de handlers/flags com remoção de wrappers inline residuais, sem alteração de API/RBAC.
- **Cleanup residual de composição em Trading/Chat (10/03/2026):** `apps/frontend-service/src/pages/TradingContent.tsx` recebeu redução de densidade da composição de `section-props/options` via contextos compartilhados de i18n e mercado/símbolo, e `apps/frontend-service/src/pages/Chat/useChatPageLayoutController.ts` recebeu cleanup de orquestração com callbacks nomeados e remoção de wrappers inline redundantes, sem alteração de API/RBAC.
- **Auditoria anti-fragmentação (400 commits) (10/03/2026):** análise de churn confirmou hotspot em `apps/frontend-service/src/pages/wise-payments` (193 arquivos TS/TSX, 14.245 linhas); baseline de execução atualizado para evitar nova micro-fragmentação e priorizar fechamento dos residuais críticos de `TradingContent` e `Chat`.
- **Decomposição incremental de catalog params/balances header em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-catalog-params-fields.tsx` passou a delegar apresentação para `wise-catalog-path-param-inputs.tsx` e `wise-catalog-query-param-controls.tsx`; `apps/frontend-service/src/pages/wise-payments/components/wise-balances-header.tsx` passou a delegar o fluxo modal de criação para `wise-balances-new-balance-dialog.tsx` e `wise-balances-new-balance-form-fields.tsx`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental de SCA/simulations operation em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-sca-tab-content.tsx` passou a delegar apresentação para `wise-sca-toolbar.tsx` e `wise-sca-payload-card.tsx` com contratos em `wise-sca-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-simulations-operation-card.tsx` passou a delegar blocos para `wise-simulations-operation-select.tsx`, `wise-simulations-operation-fields.tsx` e `wise-simulations-operation-response.tsx`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental de webhooks/card-orders actions em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-webhooks-tab-content.tsx` passou a delegar apresentação para `wise-webhooks-toolbar.tsx`, `wise-webhooks-create-card.tsx`, `wise-webhooks-delete-card.tsx` e `wise-webhooks-response-card.tsx` com contratos em `wise-webhooks-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-card-orders-actions-card.tsx` passou a delegar blocos para `wise-card-orders-order-reference-row.tsx`, `wise-card-orders-json-action-block.tsx` e `wise-card-orders-actions-footer.tsx` com contratos em `wise-card-orders-actions-card-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental de exchange quote/spend-limits em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-exchange-quote-form-card.tsx` passou a delegar apresentação para `wise-exchange-quote-form-fields.tsx` e `wise-exchange-quote-result-card.tsx`; `apps/frontend-service/src/pages/wise-payments/components/wise-spend-limits-tab-content.tsx` passou a delegar apresentação para `wise-spend-limits-fetch-controls.tsx`, `wise-spend-limits-update-panels.tsx` e `wise-spend-limits-response-panels.tsx` com contratos em `wise-spend-limits-tab-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental das tabs de kyc/simulations em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-kyc-tab-content.tsx` passou a delegar apresentação para `wise-kyc-toolbar.tsx`, `wise-kyc-evidences-card.tsx`, `wise-kyc-upload-card.tsx` e `wise-kyc-reviews-card.tsx` com contratos em `wise-kyc-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-simulations-tab-content.tsx` passou a delegar apresentação para `wise-simulations-toolbar.tsx` e `wise-simulations-operation-card.tsx` com contratos em `wise-simulations-tab-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental do gate de status em Trading (10/03/2026):** early-returns de loading/erro/not-configured/tenant-required foram extraídos de `apps/frontend-service/src/pages/TradingContent.tsx` para `apps/frontend-service/src/components/trading/TradingStatusGate.tsx` via `resolveTradingStatusGate(...)`, mantendo comportamento e reduzindo densidade do container.
- **Decomposição incremental das tabs de quotes/statements em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-quotes-tab-content.tsx` passou a delegar apresentação para `wise-quotes-form-card.tsx` e `wise-quotes-result-card.tsx` com contratos em `wise-quotes-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-statements-tab-content.tsx` passou a delegar apresentação para `wise-statements-filter-card.tsx` e `wise-statements-result-card.tsx` com contratos em `wise-statements-tab-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental das tabs de cards/card-transactions em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-cards-tab-content.tsx` passou a delegar apresentação para `wise-cards-toolbar.tsx` e `wise-cards-list-card.tsx` com contratos em `wise-cards-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-card-transactions-tab-content.tsx` passou a delegar apresentação para `wise-card-transactions-toolbar.tsx` e `wise-card-transactions-fetch-card.tsx` com contratos em `wise-card-transactions-tab-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental das tabs de account-details/catalog em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-account-details-tab-content.tsx` passou a delegar apresentação para `wise-account-details-toolbar.tsx`, `wise-account-details-create-card.tsx`, `wise-account-details-list-card.tsx`, `wise-account-details-orders-card.tsx` e `wise-recipient-requirements-card.tsx` com contratos em `wise-account-details-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-catalog-tab-content.tsx` passou a delegar apresentação para `wise-catalog-operation-config.tsx`, `wise-catalog-params-fields.tsx` e `wise-catalog-execution-panel.tsx` com contratos em `wise-catalog-tab-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental das tabs de transfers/exchange em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-transfers-tab-content.tsx` passou a delegar apresentação para `wise-transfers-header.tsx`, `wise-transfers-list-card.tsx` e `wise-transfers-actions-card.tsx` com contratos em `wise-transfers-tab-types.ts`; `apps/frontend-service/src/pages/wise-payments/components/wise-exchange-tab-content.tsx` passou a delegar apresentação para `wise-exchange-quote-form-card.tsx` e `wise-exchange-rates-card.tsx` com contratos em `wise-exchange-tab-types.ts`, mantendo paridade funcional e reduzindo densidade de UI.
- **Decomposição incremental da tab de balances em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-balances-tab-content.tsx` passou a delegar apresentação para `wise-balances-header.tsx`, `wise-balances-grid.tsx`, `wise-balance-capacity-card.tsx` e `wise-total-funds-card.tsx`; contratos foram centralizados em `wise-balances-tab-types.ts` para evitar drift de tipagem.
- **Decomposição incremental da tab de disputes em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-disputes-tab-content.tsx` passou a delegar apresentação para `wise-disputes-toolbar.tsx`, `wise-dispute-reasons-card.tsx`, `wise-dispute-flow-card.tsx`, `wise-dispute-upload-card.tsx`, `wise-dispute-status-update-card.tsx` e `wise-disputes-list-card.tsx`; contratos foram centralizados em `wise-disputes-tab-types.ts` para evitar drift de tipagem.
- **Decomposição incremental da tab de spend-controls em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-spend-controls-tab-content.tsx` passou a delegar apresentação para `wise-spend-controls-toolbar.tsx`, `wise-spend-controls-create-card.tsx`, `wise-spend-controls-assign-card.tsx`, `wise-spend-controls-delete-card.tsx` e `wise-spend-controls-list-card.tsx`; contratos foram centralizados em `wise-spend-controls-tab-types.ts` para evitar drift de tipagem.
- **Decomposição incremental da tab de recipients em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-recipients-tab-content.tsx` passou a delegar apresentação para `wise-recipients-header.tsx`, `wise-recipients-list-card.tsx`, `wise-card-permissions-card.tsx` e `wise-card-secure-card.tsx`; contratos foram centralizados em `wise-recipients-tab-types.ts` para evitar drift de tipagem.
- **Decomposição incremental da tab de card orders em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/components/wise-card-orders-tab-content.tsx` passou a delegar apresentação para `wise-card-orders-toolbar.tsx`, `wise-card-orders-create-card.tsx`, `wise-card-orders-actions-card.tsx` e `wise-card-orders-list-card.tsx`; contratos foram centralizados em `wise-card-orders-tab-types.ts` para evitar drift de tipagem.
- **Decomposição incremental de hooks de order/dispute em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-order-mutations.ts` passou a delegar mutações por subdomínio para `use-wise-account-details-order-mutation.ts`, `use-wise-card-order-write-mutations.ts` e `use-wise-card-order-read-mutations.ts`; `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-actions.ts` passou a consumir `use-wise-account-card-dispute-state.ts` para centralizar estado local e reduzir densidade do orchestration hook.
- **Decomposição incremental do composition root de WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-page-composition.ts` passou a delegar suites dedicadas `use-wise-actions-suite.ts` (wiring de ações por domínio) e `use-wise-refresh-derived.ts` (refresh + dados derivados), mantendo o contrato consumido por `apps/frontend-service/src/pages/WisePayments.tsx`.
- **Decomposição incremental de handlers de spend/card em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-card-spend-actions.ts` passou a atuar como composition root fino, enquanto os handlers foram segmentados para `use-wise-card-spend-control-handlers.ts` (card status/spend-controls) e `use-wise-card-spend-limits-handlers.ts` (spend-limits profile/card), sem alteração funcional.
- **Decomposição incremental de contratos de catálogo em WisePayments (10/03/2026):** tipos compartilhados de catálogo (`WiseCatalogOperation`, `WiseCatalogParamKey`, `WiseCatalogParams`) foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-catalog-types.ts`; `apps/frontend-service/src/pages/wise-payments/use-wise-catalog-workbench.ts` e `apps/frontend-service/src/pages/wise-payments/components/wise-catalog-tab-content.tsx` passaram a consumir o contrato único, removendo duplicações locais sem alteração funcional.
- **Decomposição incremental de composição final de tabs/constants em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-tab-props.ts` passou a atuar como composition root fino e delegar montagem para `build-wise-tab-profile-props.ts` e `build-wise-tab-operational-props.ts`; contratos foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-tab-props-types.ts` e `build-wise-profile-scoped-tab-props.ts`; `apps/frontend-service/src/pages/wise-payments/wise-payments-constants.tsx` passou a atuar como barrel com segmentação por domínio em `wise-catalog-operations.ts`, `wise-currency-options.ts` e `wise-status-badge.tsx`.
- **Decomposição incremental das props profile-scoped de tabs em WisePayments por domínio (10/03/2026):** builders foram repartidos para `apps/frontend-service/src/pages/wise-payments/build-wise-profile-core-tabs-props.ts` e `apps/frontend-service/src/pages/wise-payments/build-wise-profile-compliance-tabs-props.ts`, com tipos compartilhados em `apps/frontend-service/src/pages/wise-payments/wise-profile-tabs-props-types.ts` e `apps/frontend-service/src/pages/wise-payments/build-wise-profile-tabs-props.ts` como composition root fino.
- **Decomposição incremental das props operacionais de tabs em WisePayments por domínio (10/03/2026):** builders foram repartidos para `apps/frontend-service/src/pages/wise-payments/build-wise-operational-finance-tabs-props.ts` e `apps/frontend-service/src/pages/wise-payments/build-wise-operational-admin-tabs-props.ts`, com tipos compartilhados em `apps/frontend-service/src/pages/wise-payments/wise-operational-tabs-props-types.ts` e `apps/frontend-service/src/pages/wise-payments/build-wise-operational-tabs-props.ts` como composition root fino.
- **Decomposição incremental de handlers account/dispute/KYC por subdomínio em WisePayments (10/03/2026):** handlers foram repartidos para `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-order-action-handlers.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-dispute-kyc-action-handlers.ts`, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-actions.ts` como composition root fino de estado + wiring.
- **Decomposição incremental de mutações dispute/KYC por subdomínio em WisePayments (10/03/2026):** mutações foram repartidas para `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-flow-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-kyc-mutations.ts`, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-dispute-kyc-mutations.ts` como composition root fino.
- **Decomposição incremental de mutações account/card order por subdomínio em WisePayments (10/03/2026):** mutações foram repartidas para `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-order-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-card-transaction-mutation.ts`, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-card-order-mutations.ts` como composition root fino.
- **Decomposição incremental das operações transfer/card por subdomínio em WisePayments (10/03/2026):** operações foram repartidas para `apps/frontend-service/src/pages/wise-payments/use-wise-transfer-operations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-card-permission-secure-operations.ts`, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-transfer-and-card-operations.ts` como composition root fino.
- **Decomposição incremental das mutações card/spend por subdomínio em WisePayments (10/03/2026):** operações foram repartidas para `apps/frontend-service/src/pages/wise-payments/use-wise-card-status-mutations.ts`, `apps/frontend-service/src/pages/wise-payments/use-wise-spend-control-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-spend-limits-mutations.ts`, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-card-spend-mutations.ts` como composition root fino.
- **Decomposição incremental das mutações webhook/simulation/SCA por subdomínio em WisePayments (10/03/2026):** operações foram repartidas para `apps/frontend-service/src/pages/wise-payments/use-wise-webhook-mutations.ts`, `apps/frontend-service/src/pages/wise-payments/use-wise-simulation-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-sca-mutations.ts`, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-webhook-simulation-sca-mutations.ts` como composition root fino.
- **Decomposição incremental das queries de dados Wise por escopo (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-global-data-queries.ts` passou a centralizar queries globais e `apps/frontend-service/src/pages/wise-payments/use-wise-profile-scoped-data-queries.ts` passou a centralizar queries profile-scoped, mantendo `apps/frontend-service/src/pages/wise-payments/use-wise-data-queries.ts` como composition root fino de estado/guard/erro.
- **Decomposição incremental das mutações account/card/dispute por subdomínio em WisePayments (10/03/2026):** o domínio foi repartido em `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-card-order-mutations.ts` (account/card order/transaction) e `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-dispute-kyc-mutations.ts` (dispute/kyc), com `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-mutations.ts` mantendo apenas composição fina do contrato.
- **Decomposição incremental das ações de transfer/card em WisePayments (10/03/2026):** contratos foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-transfer-and-card-types.ts`, lógica operacional foi desacoplada para `apps/frontend-service/src/pages/wise-payments/use-wise-transfer-and-card-operations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-transfer-and-card-actions.ts` passou a focar em estado/composição.
- **Decomposição incremental das ações de balance/exchange/statement em WisePayments (10/03/2026):** contratos/defaults foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-balance-exchange-statement-types.ts`, mutações foram desacopladas para `apps/frontend-service/src/pages/wise-payments/use-wise-balance-exchange-statement-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-balance-exchange-statement-actions.ts` passou a focar em estado/handlers.
- **Decomposição incremental das ações de webhook/simulation/SCA em WisePayments (10/03/2026):** contratos/defaults foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-webhook-simulation-sca-types.ts`, mutações foram desacopladas para `apps/frontend-service/src/pages/wise-payments/use-wise-webhook-simulation-sca-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-webhook-simulation-sca-actions.ts` passou a focar em estado/handlers.
- **Decomposição incremental das ações de spend/card em WisePayments (10/03/2026):** contratos compartilhados foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-card-spend-types.ts`, mutações foram desacopladas para `apps/frontend-service/src/pages/wise-payments/use-wise-card-spend-mutations.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-card-spend-actions.ts` passou a focar em estado/handlers.
- **Decomposição incremental da composição de queries em WisePayments (10/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-query-hooks.ts` passou a centralizar hooks reutilizáveis de consulta (`useWiseApiQuery` e `useWiseProfileScopedQuery`), e `apps/frontend-service/src/pages/wise-payments/use-wise-data-queries.ts` passou a consumir esse boundary para reduzir duplicação de `useQuery/queryFn` mantendo contratos de filtros/paginação.
- **Decomposição incremental do composition root de Trading (10/03/2026):** `apps/frontend-service/src/pages/TradingContent.tsx` passou a centralizar toda a orquestração da página de Trading (estado/queries/handlers/render) e `apps/frontend-service/src/pages/Trading.tsx` passou a atuar como wrapper fino de autenticação/autorização que monta o conteúdo somente após os guards.
- **Decomposição incremental do controller de layout em Chat (09/03/2026):** `apps/frontend-service/src/pages/Chat/useChatPageLayoutController.ts` passou a centralizar orquestração de hooks e composição de `chatPageLayoutProps`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a atuar como composition root fino consumindo o controller e renderizando `ChatPageLayout`.
- **Decomposição incremental da composição de section-props em Trading (09/03/2026):** `apps/frontend-service/src/components/trading/TradingPageSectionProps.ts` passou a centralizar composição agregada de `primary/operational/dialogs/layout section-props`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir `buildTradingPageSectionProps(...)` como boundary único de assembly de props de render e `apps/frontend-service/src/components/trading/index.ts` passou a exportar o builder via barrel.
- **Decomposição incremental das mutações account/card/dispute em WisePayments (09/03/2026):** `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-mutations.ts` passou a centralizar mutações operacionais de `account details`, `card orders`, `card transactions`, `dispute flow/upload/status` e `kyc`; `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-actions.ts` passou a operar como composition root de estado/handlers e `apps/frontend-service/src/pages/wise-payments/wise-account-card-dispute-types.ts` centralizou contratos tipados compartilhados.
- **Decomposição incremental das mutações de controle em Trading (09/03/2026):** `apps/frontend-service/src/components/trading/useTradingControlOrderMutations.ts` foi reduzido para composition root e passou a delegar para `apps/frontend-service/src/components/trading/useTradingOrderExecutionMutations.ts` (create/cancel/sync) e `apps/frontend-service/src/components/trading/useTradingRiskControlActions.ts` (risk config + handover toggle), com tipos compartilhados em `apps/frontend-service/src/components/trading/trading-control-order-types.ts`.
- **Hardening incremental de tipagem de conta/posição em Trading (09/03/2026):** `apps/frontend-service/src/components/trading/TradingDomainTypes.ts` passou a expor guards de domínio (`isFuturesPositionArray`, `isSpotAccountArray`, `isFuturesAccountOverview`, `isMarginCrossOverview`, `isMarginIsolatedOverview`); `apps/frontend-service/src/pages/Trading.tsx` e `apps/frontend-service/src/components/trading/useTradingAccountPositionState.ts` passaram a consumir esses guards e remover casts inline de payload.
- **Decomposição incremental da composição final de layout no Chat (09/03/2026):** `apps/frontend-service/src/pages/Chat/chat-page-layout-props-builder.ts` passou a centralizar o assembly tipado de props de `ChatPageLayout` por blocos (`state`, `sections`, `viewport` e `handlers`); `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir `buildChatPageLayoutProps(...)`, removendo bloco inline equivalente e eliminando cast de workspace.
- **Decomposição incremental da geração LLM de sinais em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-signal-generation-service.ts` passou a centralizar a orquestração principal de `generateTradingSignalFromLlm` (profile, análise, contexto operacional, prompt budget, execução, persistência e validação), enquanto `apps/integrations-service/src/index.ts` passou a manter wrapper fino para `generateTradingSignalFromLlmCore`.
- **Decomposição incremental da composição de handlers em Trading (09/03/2026):** `apps/frontend-service/src/components/trading/useTradingCompositeActionHandlers.ts` passou a centralizar handlers de interação/mutação (`page interactions`, `dialogs`, `scheduler`, `signal profile` e `workspace`) e `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único, removendo bloco inline equivalente.
- **Decomposição incremental do contexto operacional de sinal em Integrations (09/03/2026):** `apps/integrations-service/src/trading-signal-context-service.ts` passou a centralizar RAG/orderbook/notícias/dataset/trade plan no fluxo de geração de sinal; `apps/integrations-service/src/index.ts` passou a consumir `buildTradingSignalOperationalContext` e removeu bloco inline equivalente.
- **Decomposição incremental da orquestração de análise de sinal em Integrations (09/03/2026):** `apps/integrations-service/src/trading-signal-analysis-orchestration-service.ts` passou a centralizar `analysisMatrix`, consenso, arbitragem triangular e ensemble no fluxo de geração de sinais; `apps/integrations-service/src/index.ts` passou a consumir `buildTradingSignalAnalysisContext` e removeu bloco inline equivalente.
- **Decomposição incremental do orçamento de prompt LLM em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-prompt-service.ts` passou a centralizar `buildTradingSignalPromptBudget` (composição de prompt multi-timeframe, cálculo de budget e redução progressiva de notícias); `apps/integrations-service/src/index.ts` passou a consumir esse boundary e removeu bloco inline equivalente.
- **Decomposição incremental da persistência de sinal LLM em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-signal-persistence-service.ts` passou a centralizar montagem/persistência do payload final de `createSignal` (metadata completa com técnicas/consenso/arbitragem/analysisMatrix); `apps/integrations-service/src/index.ts` passou a consumir `createTradingLlmSignalPersistenceService` e removeu bloco inline equivalente.
- **Decomposição incremental da validação final de sinal LLM em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-validation-finalize-service.ts` passou a centralizar seleção de snapshot, `validateAndPersist` e update de metadata (`validationSummary`); `apps/integrations-service/src/index.ts` passou a consumir `createTradingLlmValidationFinalizeService` e removeu bloco inline equivalente.
- **Decomposição incremental do pós-processamento de sinal LLM em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-signal-post-processing-service.ts` passou a centralizar promoção direcional por consenso e geração de `deterministicOverride`; `apps/integrations-service/src/index.ts` passou a consumir `createTradingLlmSignalPostProcessingService` e removeu bloco inline equivalente.
- **Decomposição incremental da execução GPU/LLM de sinais em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-execution-service.ts` passou a centralizar timeout/retries/backoff, fallback gateway/GPU Manager, validação de adapter LoRA e extração de conteúdo do structured output; `apps/integrations-service/src/index.ts` passou a consumir `createTradingLlmExecutionService` e removeu bloco inline equivalente.
- **Decomposição incremental do fluxo legacy institucional de sinais em Integrations (09/03/2026):** `apps/integrations-service/src/trading-legacy-institutional-signal-service.ts` passou a centralizar o branch legado de geração (`portfolio_auto` + fallback por candidatos); `apps/integrations-service/src/index.ts` passou a consumir `createTradingLegacyInstitutionalSignalService` e removeu bloco inline equivalente.
- **Decomposição incremental da análise técnica persistida em Integrations (09/03/2026):** `apps/integrations-service/src/trading-technical-analysis-service.ts` passou a centralizar cálculo técnico, ensemble e persistência em `tradingTechnicalIndicators`; `apps/integrations-service/src/index.ts` passou a consumir `createTradingTechnicalAnalysisService` e removeu função inline equivalente.
- **Decomposição incremental da orquestração de dataset em Integrations (09/03/2026):** `apps/integrations-service/src/trading-dataset-orchestration-service.ts` passou a centralizar criação de datasets por `signal` e `order` (incluindo lineage e atualização de status); `apps/integrations-service/src/index.ts` passou a consumir `createTradingDatasetOrchestrationService` e removeu blocos inline equivalentes.
- **Decomposição incremental de namespace de dataset em Integrations (09/03/2026):** `apps/integrations-service/src/trading-dataset-namespace-service.ts` passou a centralizar validação de candidatos e inferência de namespace para datasets de trading; `apps/integrations-service/src/index.ts` passou a consumir `createTradingDatasetNamespaceService` nos fluxos de signal/order.
- **Decomposição incremental da seed de dataset por sinal em Integrations (09/03/2026):** `apps/integrations-service/src/trading-dataset-seed-service.ts` passou a centralizar `buildTradingDatasetSeedFromSignal`; `apps/integrations-service/src/index.ts` passou a consumir `createTradingDatasetSeedService` e removeu função inline equivalente com contratos tipados de análise/consenso/prompt.
- **Decomposição incremental de core de dataset de trading em Integrations (09/03/2026):** `apps/integrations-service/src/trading-dataset-core-service.ts` passou a centralizar embedding, deduplicação semântica, score de qualidade e helpers de ação/prompt de ordem; `apps/integrations-service/src/index.ts` passou a consumir `createTradingDatasetCoreService` e removeu funções inline equivalentes.
- **Decomposição incremental de runtime de integridade imutável em Integrations (09/03/2026):** `apps/integrations-service/src/integrations-immutable-audit-runtime-service.ts` passou a centralizar estado, execução sob demanda e scheduler da verificação de cadeia imutável; `apps/integrations-service/src/index.ts` passou a consumir `createIntegrationsImmutableAuditRuntimeService` e removeu bloco inline equivalente.
- **Decomposição incremental de runtime de métricas de trading em Integrations (09/03/2026):** `apps/integrations-service/src/trading-metrics-runtime-service.ts` passou a centralizar refresh de PnL/ordens/indicadores e lifecycle do scheduler; `apps/integrations-service/src/index.ts` passou a consumir `createTradingMetricsRuntimeService` e removeu bloco inline equivalente.
- **Hardening incremental de estado de integridade imutável em Integrations (09/03/2026):** `apps/integrations-service/src/index.ts` passou a atualizar `integrationsImmutableAuditIntegrityState` in-place (`Object.assign`) para manter referência viva compartilhada com rotas core de health/audit e eliminar risco de estado stale.
- **Decomposição incremental de observabilidade de chamadas em Integrations (09/03/2026):** `apps/integrations-service/src/integration-call-observer-service.ts` passou a centralizar classificação de erro e telemetria de chamadas (`updateIntegrationMetrics`, `observeIntegrationCall`); `apps/integrations-service/src/index.ts` passou a consumir `createIntegrationCallObserverService` e removeu funções inline equivalentes.
- **Decomposição incremental de runtime de schedulers em Integrations (09/03/2026):** `apps/integrations-service/src/trading-scheduler-runtime-service.ts` passou a centralizar execução de schedulers de sinais/análise (poll, lock otimista, persistência e tratamento de erro); `apps/integrations-service/src/index.ts` passou a consumir `createTradingSchedulerRuntimeService` e removeu bloco inline equivalente com parada explícita em shutdown.
- **Decomposição incremental de escopo/perfil de trading em Integrations (09/03/2026):** `apps/integrations-service/src/trading-scope-profile-service.ts` passou a centralizar resolução de namespace Trading, resumo de datasets aprovados, validação de namespace por tenant e criação idempotente de perfil (`analysis`/`signal`); `apps/integrations-service/src/index.ts` passou a consumir `createTradingScopeProfileService` e removeu funções inline equivalentes.
- **Decomposição incremental de prompt/token budget LLM em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-prompt-service.ts` passou a centralizar construção de prompt multi-timeframe e orçamento de tokens (`buildMultiTimeframePrompt`, `resolveMaxTokensForPrompt`); `apps/integrations-service/src/index.ts` passou a consumir `createTradingLlmPromptService` e removeu constantes/funções inline equivalentes.
- **Decomposição incremental de normalização de sinal LLM em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-signal-normalizer-service.ts` passou a centralizar normalização numérica/cited values e montagem validada de sinal (`buildLlmSignalFromPartial`); `apps/integrations-service/src/index.ts` passou a consumir `createTradingLlmSignalNormalizerService` e removeu funções inline equivalentes.
- **Decomposição incremental de trade-plan determinístico em Integrations (09/03/2026):** `apps/integrations-service/src/trading-signal-plan-service.ts` passou a centralizar signal type, motivadores, invalidação, SL/TP e montagem de trade plan; `apps/integrations-service/src/index.ts` passou a consumir funções dedicadas e removeu implementações inline equivalentes.
- **Decomposição incremental de contexto de agente/scheduler em Integrations (09/03/2026):** `apps/integrations-service/src/trading-agent-context-service.ts` passou a centralizar settings agentic, resolução de agente/namespace Trading, resolução de usuário do scheduler e montagem do system prompt de sinais; `apps/integrations-service/src/index.ts` passou a consumir `createTradingAgentContextService` e removeu funções inline equivalentes.
- **Decomposição incremental de arbitragem triangular em Integrations (09/03/2026):** `apps/integrations-service/src/trading-arbitrage-service.ts` passou a centralizar snapshot de order book, resolução de legs e cálculo de arbitragem com fees por exchange (`feePctByExchange`) e network fees por ativo; `apps/integrations-service/src/index.ts` passou a consumir `createTradingArbitrageService` e removeu funções inline equivalentes.
- **Decomposição incremental de config de perfil em Integrations (09/03/2026):** `apps/integrations-service/src/trading-profile-config-service.ts` passou a centralizar parse/normalização de perfil de trading, validação de arbitragem e `TradingConfigError`; `apps/integrations-service/src/index.ts` passou a consumir `createTradingProfileConfigService` e removeu funções/classes inline equivalentes.
- **Decomposição incremental de consenso/ensemble em Integrations (09/03/2026):** `apps/integrations-service/src/trading-analysis-consensus-service.ts` passou a centralizar consenso majoritário, agregação de scores por técnica e resultado de ensemble; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado e removeu funções inline equivalentes.
- **Decomposição incremental de suporte de sinal em Integrations (09/03/2026):** `apps/integrations-service/src/trading-signal-support-service.ts` passou a centralizar split de símbolo, derivação de ativos intermediários, mapeamento de erro amigável e símbolo padrão por mercado; `apps/integrations-service/src/index.ts` passou a consumir `createTradingSignalSupportService` e removeu funções inline equivalentes.
- **Decomposição incremental do serviço de notícias de trading em Integrations (09/03/2026):** `apps/integrations-service/src/trading-news-service.ts` passou a centralizar normalização de config, construção de query e consulta de notícias via RAG web-search; `apps/integrations-service/src/index.ts` passou a consumir `createTradingNewsService` e removeu funções inline equivalentes.
- **Decomposição incremental do serviço de market context em Integrations (09/03/2026):** `apps/integrations-service/src/trading-market-context-service.ts` passou a centralizar candles/indicadores/contexto de mercado para datasets e sinais; `apps/integrations-service/src/index.ts` passou a consumir `createTradingMarketContextService` e removeu funções inline equivalentes.
- **Decomposição incremental do serviço de fees KuCoin em Integrations (09/03/2026):** `apps/integrations-service/src/kucoin-trading-fee-service.ts` passou a centralizar cálculo/cache de trade fees, network fees por ativo e fallback persistido por tenant em arbitragem; `apps/integrations-service/src/index.ts` passou a consumir `createKucoinTradingFeeService` e removeu funções inline equivalentes.
- **Decomposição incremental do catálogo de símbolos de trading em Integrations (09/03/2026):** `apps/integrations-service/src/trading-symbol-catalog-service.ts` passou a centralizar normalização/listagem de símbolos, seleção por universo de candidatos e catálogo de auto-assets por venue; `apps/integrations-service/src/index.ts` passou a consumir `createTradingSymbolCatalogService` e removeu funções inline equivalentes.
- **Decomposição incremental da resolução de tenant WS privado KuCoin em Integrations (09/03/2026):** `apps/integrations-service/src/kucoin-private-ws-tenant-service.ts` passou a centralizar a resolução dinâmica do tenant para eventos privados do WS; `apps/integrations-service/src/index.ts` passou a consumir `createResolveKucoinTenantIdForPrivateWs` e removeu função inline equivalente.
- **Decomposição incremental dos request resolvers de trading em Integrations (09/03/2026):** `apps/integrations-service/src/trading-request-resolver-service.ts` passou a centralizar resolução/validação de requests de trading; `apps/integrations-service/src/index.ts` passou a consumir `createTradingRequestResolver` e removeu funções inline equivalentes.
- **Decomposição incremental das métricas WS KuCoin em Integrations (09/03/2026):** `apps/integrations-service/src/kucoin-ws-metrics-service.ts` passou a centralizar mapeamento de estado/wiring de listeners de métricas public/private; `apps/integrations-service/src/index.ts` passou a consumir `createKucoinWsMetricsWiring` e removeu bloco inline equivalente.
- **Decomposição incremental dos handlers de market data em Integrations (09/03/2026):** `apps/integrations-service/src/trading-market-data-handlers.ts` passou a centralizar os handlers `klines` e `orderbook`; `apps/integrations-service/src/index.ts` passou a consumir `createTradingMarketDataHandlers` e removeu funções inline equivalentes.
- **Decomposição incremental da configuração WS KuCoin em Integrations (09/03/2026):** `apps/integrations-service/src/kucoin-ws-config-service.ts` passou a centralizar depths REST/WS, parsing/validação de intervalos e registry de tópicos Spot/Margin; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado e removeu funções inline equivalentes.
- **Decomposição incremental da startup orchestration em Integrations (09/03/2026):** `apps/integrations-service/src/integration-startup-service.ts` passou a centralizar bootstrap de integrações por tenant e inicialização de caches; `apps/integrations-service/src/index.ts` passou a consumir `createIntegrationStartupOrchestrator` e removeu bloco inline equivalente de startup.
- **Decomposição incremental do auth context Wise em Integrations (09/03/2026):** `apps/integrations-service/src/wise-auth-context-service.ts` passou a centralizar `getWiseAuthContextFromRequest`; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado e removeu função inline equivalente de validação de tenant.
- **Decomposição incremental do bootstrap de canais externos em Integrations (09/03/2026):** `apps/integrations-service/src/integrations-bootstrap-service.ts` passou a centralizar `initializeGmailTransporter` e `initializeStripeClient`; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado e removeu bloco inline equivalente de startup.
- **Decomposição incremental das chamadas externas/timeout em Integrations (09/03/2026):** `apps/integrations-service/src/integration-external-call-service.ts` passou a centralizar `createExecuteStripeCall` e `withTimeout`; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado e removeu funções inline equivalentes do composition root.
- **Decomposição incremental do parser LLM de sinais em Integrations (09/03/2026):** `apps/integrations-service/src/trading-llm-signal-parser.ts` passou a centralizar parsing, normalização e repair de resposta LLM para sinais de trading; `apps/integrations-service/src/index.ts` passou a consumir `createLlmSignalResponseParser` e removeu bloco inline equivalente do composition root.
- **Decomposição incremental da idempotência de webhook em Integrations (09/03/2026):** `apps/integrations-service/src/webhook-idempotency-service.ts` passou a centralizar `checkWebhookIdempotency` e `markWebhookProcessed`; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado com wrapper de logger e removeu implementação inline equivalente.
- **Decomposição incremental de health checks em Integrations (09/03/2026):** `apps/integrations-service/src/integration-health-service.ts` passou a centralizar os checks de Stripe/Wise/Twilio/Email/OpenAI/Trading e o refresh de métricas; `apps/integrations-service/src/index.ts` passou a consumir `createIntegrationHealthRefresher` e removeu funções inline equivalentes do composition root.
- **Decomposição incremental da persistência Wise em Integrations (09/03/2026):** `apps/integrations-service/src/wise-storage-service.ts` passou a centralizar `upserts` e gravação de eventos de webhook Wise; `apps/integrations-service/src/index.ts` passou a consumir o módulo dedicado e removeu funções inline equivalentes de alto volume no composition root.
- **Decomposição incremental do fluxo Twilio/WhatsApp em Integrations (09/03/2026):** `apps/integrations-service/src/twilio-chat-media-service.ts` passou a centralizar o processamento de mensagem via Chat Service (`buildProcessMessageWithLLM`) e o processamento de mídia para RAG (`buildProcessWhatsAppMediaForRAG`); `apps/integrations-service/src/index.ts` passou a consumir esses builders e removeu funções inline equivalentes do composition root.
- **Decomposição incremental da mutação de stream em Chat (09/03/2026):** `apps/frontend-service/src/pages/Chat/useChatSendMessageMutation.ts` passou a centralizar `useMutation(createChatStreamMutationConfig(...))`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu wrappers inline de `sendMessage.mutate(...)` nos fluxos de composer, gravação, sync de mensagens e quick reply.
- **Decomposição incremental da composição de queries/filtros em WisePayments (09/03/2026):** `apps/frontend-service/src/pages/wise-payments/wise-query-builders.ts` passou a centralizar montagem de paths `profile scoped` (`profileId` + params adicionais) e fetch JSON tipado; `apps/frontend-service/src/pages/wise-payments/use-wise-data-queries.ts` passou a consumir esse boundary único, removendo duplicação de `queryFn` para `cards`, `spend-controls`, `disputes`, `kyc`, `card-orders`, `dispute-reasons` e `account-details`.
- **Decomposição incremental da invalidação de queries de conta em Trading (09/03/2026)**: `apps/frontend-service/src/components/trading/useTradingAccountInvalidation.ts` passou a centralizar a invalidação de `['account']`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único em `useTradingWorkspaceActionHandlers` e removeu callback inline equivalente.
- **Decomposição incremental dos callbacks residuais de Trading (09/03/2026)**: `apps/frontend-service/src/components/trading/useTradingKlineInvalidation.ts` e `apps/frontend-service/src/components/trading/useTradingAuthRedirect.ts` passaram a centralizar callbacks de invalidação/redirect; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esses boundaries e removeu callbacks inline equivalentes.
- **Decomposição incremental dos bindings residuais de Chat (09/03/2026)**: `apps/frontend-service/src/pages/Chat/useChatContainerBindings.ts` passou a centralizar `workspaceOptions`, `fallbackMessageUser`, callbacks de `approval policy`/exclusão e sync de foco por conversa; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu hooks inline equivalentes.
- **Decomposição incremental da composição de tabs em WisePayments (09/03/2026)**: `apps/frontend-service/src/pages/wise-payments/use-wise-tab-props.ts` passou a operar como composition root fino e delegar o assembly para `build-wise-profile-tabs-props.ts` (tabs com `profile scope`) e `build-wise-operational-tabs-props.ts` (tabs operacionais), mantendo contratos.
- **Decomposição incremental dos wrappers operacionais de apresentação em Trading (09/03/2026)**: `criticalApiError`, `renderOrderStatusBadge`, `renderSignalTypeBadge` e `wsHealthy` foram centralizados em `apps/frontend-service/src/components/trading/useTradingOperationalPresentationWrappers.tsx`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu composição inline equivalente.
- **Decomposição incremental da sincronização de scheduler em Trading (09/03/2026)**: hidratação de `schedulerForm` por `schedulerConfig` foi centralizada em `apps/frontend-service/src/components/trading/useTradingSchedulerFormSync.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu `useEffect` inline equivalente.
- **Decomposição incremental da subscription futures de quotes em Trading (09/03/2026)**: side-effect de subscribe/unsubscribe de tickers para posições abertas futures foi centralizado em `apps/frontend-service/src/components/trading/useTradingFuturesQuoteSubscription.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu `useEffect` inline equivalente.
- **Decomposição incremental dos handlers realtime em Trading (09/03/2026)**: handlers de `onError/onTicker/onOrderUpdate/onPositionUpdate/onBalance` foram centralizados em `apps/frontend-service/src/components/trading/useTradingRealtimeEventHandlers.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu handlers inline equivalentes.
- **Decomposição incremental dos derivados de payload/candidatos em Trading (09/03/2026)**: `topTradingCandidates`, `signalProfilePayload` e `isSignalProfilePayloadComplete` foram centralizados em `apps/frontend-service/src/components/trading/useTradingDerivedPayloadState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu memoizações inline equivalentes.
- **Decomposição incremental da conexão realtime em Trading (09/03/2026)**: estado derivado de `symbol/request/wsEnabled/wsChannels` foi centralizado em `apps/frontend-service/src/components/trading/useTradingRealtimeConnectionState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu composição inline equivalente.
- **Decomposição incremental da navegação/opções em Trading (09/03/2026)**: memoizações e callbacks de `workspaces/tabs/modes/indicators/techniques` foram centralizados em `apps/frontend-service/src/components/trading/useTradingNavigationPresentation.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu composição inline equivalente.
- **Decomposição incremental do estado local em Trading (09/03/2026)**: estado local de UI/forms/dialogs/execução e refs de autosave foram centralizados em `apps/frontend-service/src/components/trading/useTradingLocalState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu declarações inline equivalentes de `useState/useRef`.
- **Decomposição incremental do estado local em Chat (09/03/2026)**: estado local de UI/stream/diálogos/áudio foi centralizado em `apps/frontend-service/src/pages/Chat/useChatLocalState.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu declarações inline equivalentes de `useState/useRef`.
- **Decomposição incremental do shell de layout em Chat (09/03/2026)**: composição principal de `sidebar/header/workspace/messages/composer/dialogs` foi centralizada em `apps/frontend-service/src/pages/Chat/components/ChatPageLayout.tsx`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu composição inline equivalente.
- **Decomposição incremental de workspace e composer em Chat (09/03/2026)**: `WorkspaceFilterBar` e o formulário/composer (`motion.form` + `ChatInput`) foram centralizados em `apps/frontend-service/src/pages/Chat/components/ChatWorkspaceSection.tsx` e `apps/frontend-service/src/pages/Chat/components/ChatComposerSection.tsx`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esses boundaries e removeu blocos inline equivalentes.
- **Decomposição incremental da sidebar de conversas em Chat (09/03/2026)**: lateral de conversas (drawer mobile + sidebar desktop animada) foi centralizada em `apps/frontend-service/src/pages/Chat/components/ChatConversationsSidebar.tsx`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu bloco inline equivalente.
- **Decomposição incremental do header responsivo em Chat (09/03/2026)**: header desktop/mobile com governança e ações foi centralizado em `apps/frontend-service/src/pages/Chat/components/ChatHeaderSection.tsx`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu bloco inline equivalente.
- **Decomposição incremental das seções principais de Trading (09/03/2026)**: renderização de alertas/header/métricas/tabs/dialogs foi centralizada em `apps/frontend-service/src/components/trading/TradingPageSections.tsx`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu bloco inline equivalente.
- **Decomposição incremental do viewport de mensagens em Chat (09/03/2026)**: renderização de mensagens com `ScrollArea`, seleção, hints/banners e diagnóstico de stream foi centralizada em `apps/frontend-service/src/pages/Chat/components/ChatMessagesViewport.tsx`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu bloco inline equivalente.
- **Decomposição incremental da query de mensagens em Chat (09/03/2026)**: o `queryFn` de mensagens por conversa (`/api/chat/conversations/:id/messages`) foi centralizado em `apps/frontend-service/src/pages/Chat/useChatQueryState.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir o hook apenas com `conversationId`, removendo callback inline de fetch.
- **Decomposição incremental das queries de conversas do Chat (09/03/2026)**: fetch paginado por cursor, estado de paginação/loading e derivação de `conversations/activeConversation` foram centralizados em `apps/frontend-service/src/pages/Chat/useChatConversationsQueryState.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu bloco inline equivalente de `useInfiniteQuery`.
- **Decomposição incremental da composição de WisePayments (09/03/2026)**: a orquestração de navegação, queries, actions, `derivedData`, `refreshActions` e `tabsContentProps` foi centralizada em `apps/frontend-service/src/pages/wise-payments/use-wise-page-composition.ts`; `apps/frontend-service/src/pages/WisePayments.tsx` passou a consumir esse boundary único e removeu composição inline equivalente.
- **Decomposição incremental da composição de layout em Trading (09/03/2026)**: assembly tipado de `operationalAlertsSectionProps`, `headerSectionProps`, `statsPrimarySectionProps`, `statsSecondarySectionProps` e `tabsShellSectionProps` foi centralizado em `buildTradingLayoutSectionProps` no arquivo `apps/frontend-service/src/components/trading/TradingSectionPropsBuilders.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu blocos inline equivalentes.
- **Decomposição incremental da composição de abas primárias em Trading (09/03/2026)**: assembly tipado de `analysis/lab/orders/overview/portfolio-auto/positions/signals-auto/signals` foi centralizado em `buildTradingPrimaryTabsSectionProps` no arquivo `apps/frontend-service/src/components/trading/TradingSectionPropsBuilders.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu blocos inline equivalentes.
- **Decomposição incremental da composição de seções do Chat (09/03/2026)**: assembly tipado de `conversationsListProps`, `chatActionsMenuProps`, `chatGovernanceControlsProps` e `chatDialogsSectionProps` foi centralizado em `apps/frontend-service/src/pages/Chat/useChatSectionProps.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir o novo boundary único e removeu blocos inline equivalentes.
- **Decomposição incremental da composição de abas operacionais/dialogs em Trading (09/03/2026)**: builders tipados por `ComponentProps` foram centralizados em `apps/frontend-service/src/components/trading/TradingSectionPropsBuilders.ts` (`buildTradingOperationalTabsSectionProps` e `buildTradingDialogsSectionProps`); `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esses builders e removeu blocos inline equivalentes.
- **Decomposição incremental da derivação de conta/posições em Trading (09/03/2026)**: estado derivado de conta/posições e resumos operacionais (`accountMode`, `spot/margin positions`, `openPositionsCount`, `futures/spot/margin summaries` e `quoteCurrency`) foi centralizado em `apps/frontend-service/src/components/trading/useTradingAccountPositionState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu bloco inline equivalente.
- **Decomposição incremental das queries de Chat (09/03/2026)**: consultas e estado derivado de dados (`conversationMessages`, `approvalPolicy`, `version`, `assistant settings`, `namespaces` e `agents`) foram centralizados em `apps/frontend-service/src/pages/Chat/useChatQueryState.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu blocos inline equivalentes de `useQuery`.
- **Decomposição incremental das coleções de símbolos/candidatos em Trading (09/03/2026)**: coleções derivadas de símbolos (`symbolOptions` e `symbolSelectItems` agrupados por destaque/favoritos) foram centralizadas em `apps/frontend-service/src/components/trading/useTradingSymbolCandidateViewState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu blocos inline equivalentes.
- **Decomposição incremental da apresentação de sinais em Trading (09/03/2026)**: estado derivado de apresentação de sinais/mercado (`intervalOptions`, fontes, validação de arbitragem, `wsInterval`/`granularity` e depths) foi centralizado em `apps/frontend-service/src/components/trading/useTradingSignalPresentationState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu blocos inline equivalentes.
- **Decomposição incremental da apresentação de workspaces em Chat (09/03/2026)**: estado derivado de apresentação (`workspaceHint`, `agentOptions`, badge de versão e flags de controles) foi centralizado em `apps/frontend-service/src/pages/Chat/useChatWorkspacePresentation.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu blocos inline equivalentes.
- **Decomposição incremental da sincronização de mensagens em Chat (09/03/2026)**: sincronização de mensagens carregadas da conversa e flush de envio pendente pós-streaming foram centralizados em `apps/frontend-service/src/pages/Chat/useChatMessageSyncEffects.ts`; `apps/frontend-service/src/pages/Chat/index.tsx` passou a consumir esse boundary único e removeu dois `useEffect`s inline equivalentes.
- **Decomposição incremental do bootstrap de estado em Trading (09/03/2026)**: sincronizações de bootstrap (`default portfolio`, normalização de `autoMix/all modes`, defaults de `symbol/interval` e fee efetivo de arbitragem) foram centralizadas em `apps/frontend-service/src/components/trading/useTradingBootstrapStateSync.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu seis `useEffect`s inline equivalentes.
- **Decomposição incremental da série de klines em Trading (09/03/2026)**: composição de série de candles (WS/REST), deduplicação por assinatura e fallback de continuidade visual foram centralizados em `apps/frontend-service/src/components/trading/useTradingKlineSeriesState.ts`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e removeu estado/efeitos inline equivalentes.
- **Decomposição incremental de queries realtime/permissões em Trading (09/03/2026)**: queries de `klines`/`orderbook` foram centralizadas em `apps/frontend-service/src/components/trading/useTradingMarketRealtimeQueries.ts` e a query RBAC do wrapper em `apps/frontend-service/src/components/trading/useTradingPermissionsQuery.ts`; com isso, `apps/frontend-service/src/pages/Trading.tsx` passou a operar sem `useQuery` inline.
- **Decomposição incremental de queries de símbolos/assets em Trading (09/03/2026)**: queries de `symbols` e `auto assets catalog`, junto com derivações de símbolos favoritos/destaques e mapa/opções de assets automáticos, foram centralizadas em `apps/frontend-service/src/components/trading/useTradingSymbolAssetQueries.ts`, reduzindo acoplamento no container `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental de queries de setup em Trading (09/03/2026)**: queries de bootstrap/automação (`status`, `portfolios`, `candidates`, `rebalances`, `auto runs`, `intervals`, `analysis-profile` e `arbitrage catalog`) foram centralizadas em `apps/frontend-service/src/components/trading/useTradingSetupQueries.ts`, reduzindo acoplamento no container `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental de queries operacionais em Trading (09/03/2026)**: queries de `ws status`, `risk-config` e `control-history` foram centralizadas em `apps/frontend-service/src/components/trading/useTradingOperationalQueries.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental de queries de post-mortem/training em Trading (09/03/2026)**: queries de post-mortem real, namespaces ativos e rastreio de envio para treinamento foram centralizadas em `apps/frontend-service/src/components/trading/useTradingPostmortemTrainingQueries.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental do bloco de tabs em WisePayments (09/03/2026)**: a renderização de tabs foi centralizada em `apps/frontend-service/src/pages/wise-payments/components/wise-payments-tabs-content.tsx`, reduzindo acoplamento de apresentação no container `apps/frontend-service/src/pages/WisePayments.tsx`.
- **Decomposição incremental da composição tipada de tabs em WisePayments (09/03/2026)**: o assembly de contratos para `WisePaymentsTabsContent` foi centralizado em `apps/frontend-service/src/pages/wise-payments/use-wise-tab-props.ts`, com export de `WisePaymentsTabsContentProps` em `apps/frontend-service/src/pages/wise-payments/components/wise-payments-tabs-content.tsx`, reduzindo densidade do composition root `apps/frontend-service/src/pages/WisePayments.tsx`.
- **Decomposição incremental do auto-save de signal profile em Trading (09/03/2026)**: a hidratação de `signalProfileResponse` e persistência debounced do payload de signal profile foram centralizadas em `apps/frontend-service/src/components/trading/useTradingSignalProfileAutoSave.ts`, reduzindo efeitos inline em `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental da sincronização de risco/review em Trading (09/03/2026)**: a hidratação de `riskConfig` (form de risco + defaults iniciais) e a abertura do diálogo de revisão de ordens foram centralizadas em `apps/frontend-service/src/components/trading/useTradingRiskReviewState.ts`, reduzindo efeitos/handlers inline em `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental da derivação de market/orderbook em Trading (09/03/2026)**: a derivação de `market`, `orderBookData`, `orderBookPrecision` e a invalidação de klines por troca de contexto foram centralizadas em `apps/frontend-service/src/components/trading/useTradingMarketOrderBookState.ts`, reduzindo bloco inline em `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental dos handlers de interação de UI em Chat (09/03/2026)**: callbacks de drawer/sidebar, seleção, diagnóstico de stream, diálogos de treino/exclusão e quick-reply foram centralizados em `apps/frontend-service/src/pages/Chat/useChatUiInteractionHandlers.ts`, reduzindo callbacks inline em `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Decomposição incremental de queries de sinais/scheduler em Trading (09/03/2026)**: as queries de `signals` e `signal-scheduler`, com reconciliação de seleção e composição de scheduler, foram centralizadas em `apps/frontend-service/src/components/trading/useTradingSignalSchedulerQueries.ts`, reduzindo acoplamento do container `apps/frontend-service/src/pages/Trading.tsx`.
- **Decomposição incremental de queries centrais de Trading (09/03/2026)**: as queries de mercado/conta/posições/ordens foram centralizadas em `apps/frontend-service/src/components/trading/useTradingMarketAccountQueries.ts`, reduzindo acoplamento do container `apps/frontend-service/src/pages/Trading.tsx` sem alterar contratos.
- **Decomposição incremental dos diálogos do Chat (09/03/2026)**: os diálogos operacionais de treinamento/exclusão foram centralizados em `apps/frontend-service/src/pages/Chat/components/ChatDialogsSection.tsx`, com integração por contrato único em `apps/frontend-service/src/pages/Chat/index.tsx`, mantendo comportamento e contratos de API/RBAC.
- **Auditoria de Observabilidade (13/01/2026)**: conteúdo consolidado no `docs/OBSERVABILITY.md` para evitar duplicidade de informações.
- **Alinhamento documentação com código (02/2026)**: README (estrutura `apps/` com biometrics, llm-gateway, gpu-manager; contagem 17 imagens release), ARQUITETURA (biometrics como Python FastAPI), OBSERVABILITY (targets/dashboards Biometrics e LLM Gateway, smoke test dashboards). Índice e SECRETS já refletiam LLM_GATEWAY_URL e BIOMETRICS; STATUS-REAL-ATUAL já listava todos os serviços Alice.
- **Remediação Training/Datasets (06/03/2026)**: execução do Bloco 1 (48h), plano em 3 ondas e evidências de validação registrados em `docs/RELATORIO-IMPLEMENTACAO-REMEDIACAO-TRAINING-DATASET-2026-03-06-BLOCO-1.md`.
- **Segunda Passagem Enterprise (06/03/2026)**: validação cruzada com review externa, matriz de cobertura item-a-item, hardening de dataset canônico/split/lifecycle/promoção e validações sequenciais registrados em `docs/RELATORIO-IMPLEMENTACAO-REMEDIACAO-TRAINING-DATASET-2026-03-06-BLOCO-2.md`.
- **Decomposição incremental de mutações de ordens/controle em Trading (08/03/2026)**: mutações/handover de `apps/frontend-service/src/pages/Trading.tsx` foram desacopladas para `apps/frontend-service/src/components/trading/useTradingControlOrderMutations.ts`, com reexport no barrel `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental de mutações de sinais em Trading (08/03/2026)**: mutações `create/generate/auto-run/scheduler/deactivate` foram desacopladas para `apps/frontend-service/src/components/trading/useTradingSignalMutations.ts`, com reexport no barrel `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental da orquestração de pipeline em Trading (08/03/2026)**: `enqueueTradingMutation`, `enqueueTrading` e `runPortfolioAutoPipeline` foram desacoplados para `apps/frontend-service/src/components/trading/useTradingPipelineActions.ts`, com reexport no barrel `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental das preferências de símbolos em Trading (08/03/2026)**: governança de favoritos/destaques (`updateSymbolPrefs`, `toggleFavorite`, `toggleFeatured`) foi desacoplada para `apps/frontend-service/src/components/trading/useTradingSymbolPreferences.ts`, com reexport no barrel `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental das mutações de perfil/post-mortem em Trading (08/03/2026)**: `updateSignalProfile` e `sendPostMortemToTraining` foram desacopladas para `apps/frontend-service/src/components/trading/useTradingProfilePostmortemMutations.ts`, com reexport no barrel `apps/frontend-service/src/components/trading/index.ts`.
- **Composição de queries/filtros em WisePayments desacoplada (08/03/2026)**: carregamento de dados/guard de erro/profile filter/paginação foi centralizado em `apps/frontend-service/src/pages/wise-payments/use-wise-data-queries.ts` e contratos tipados consolidados em `apps/frontend-service/src/pages/wise-payments/wise-payments-types.ts`.
- **Constantes/status de WisePayments desacopladas (08/03/2026)**: catálogo de operações, lista de moedas e renderer de status badge foram centralizados em `apps/frontend-service/src/pages/wise-payments/wise-payments-constants.tsx`, reduzindo acoplamento de `apps/frontend-service/src/pages/WisePayments.tsx`.
- **Lifecycle de conversas do Chat desacoplado (08/03/2026)**: create/delete/bulk-delete/delete-all, approval policy e handlers de sidebar/foco foram centralizados em `apps/frontend-service/src/pages/Chat/useChatConversationLifecycle.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Ações de treinamento/feedback do Chat desacopladas (08/03/2026)**: envio de conversa/mensagens para treinamento, abertura de dialogs e feedback multimodal (`rate message`/`rate image`) foram centralizados em `apps/frontend-service/src/pages/Chat/useChatTrainingFeedbackActions.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Ações de gravação/transcrição do Chat desacopladas (08/03/2026)**: start/stop/send de gravação, polling de transcrição, upload JSON e fallback de MIME foram centralizados em `apps/frontend-service/src/pages/Chat/useChatRecordingActions.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Ações de anexos de mídia do Chat desacopladas (08/03/2026)**: upload/remoção/limpeza de mídia pendente foram centralizados em `apps/frontend-service/src/pages/Chat/useChatMediaAttachmentActions.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Diagnóstico de stream do Chat desacoplado (08/03/2026)**: resolução de status, append de eventos e criação de eventos de status foram centralizados em `apps/frontend-service/src/pages/Chat/useChatStreamDiagnostics.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Filtros de conversa do Chat desacoplados (08/03/2026)**: parsing de querystring de contexto/filtro de datas e ação de limpeza foram centralizados em `apps/frontend-service/src/pages/Chat/useChatConversationFilters.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Ações do composer do Chat desacopladas (08/03/2026)**: handlers de `regenerate`, `stop streaming`, `send` e `submit` foram centralizados em `apps/frontend-service/src/pages/Chat/useChatComposerActions.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Roteamento de agentes do Chat desacoplado (08/03/2026)**: estado de `routing mode`, agentes selecionados, source/debug e validação de seleção manual foram centralizados em `apps/frontend-service/src/pages/Chat/useChatRoutingState.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Lifecycle de página do Chat desacoplado (08/03/2026)**: side-effects de drawer mobile, sincronização de refs de input/mídia, reset de sync por conversa e cleanup de gravação foram centralizados em `apps/frontend-service/src/pages/Chat/useChatPageLifecycle.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Stream mutation do Chat desacoplado (08/03/2026)**: mutação principal de `sendMessage` (stream SSE, parsing, atualizações incrementais, roteamento e fallback de erro) foi centralizada em `apps/frontend-service/src/pages/Chat/chat-stream-mutation.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Chat/index.tsx`.
- **Handlers residuais de Trading desacoplados (08/03/2026)**: callbacks de prefill de ordem, troca de intervalo e abertura de review por id foram centralizados em `apps/frontend-service/src/components/trading/useTradingPageInteractionHandlers.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/Trading.tsx`.
- **Navegação/parser de WisePayments desacoplados (08/03/2026)**: estado de workspace/tabs e parser JSON seguro foram centralizados em `apps/frontend-service/src/pages/wise-payments/use-wise-navigation-state.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-json-parser.ts`, reduzindo acoplamento de `apps/frontend-service/src/pages/WisePayments.tsx`.
- **Decomposição de rotas do Integrations Service (06/03/2026)**: boundaries de rotas modularizados em `apps/integrations-service/src/routes/*` (Stripe, Twilio webhooks/operacional, Email, Grafana/GitHub, Core, Registry, Probes, Demo Trading e Post-Mortem), com `index.ts` como composition root e atualização de SSOT em `README.md`, `docs/ARQUITETURA.md`, `docs/STATUS-REAL-ATUAL.md` e `docs/PLANO-IMPLEMENTACAO-ENTERPRISE-BLOCOS.md`.
- **Rotas de sistema do Auth Service modularizadas (07/03/2026)**: health/readiness/liveness e `/api/auth/user` extraídos para `apps/auth-service/src/routes/auth-system-routes.ts`, mantendo `index.ts` como composition root.
- **Rotas de providers do Auth Service modularizadas (07/03/2026)**: fluxos OAuth/SAML (`/api/auth/google*`, `/api/auth/github*`, `/api/auth/saml*`) extraídos para `apps/auth-service/src/routes/auth-provider-routes.ts`.
- **Rotas de credenciais locais do Auth Service modularizadas (07/03/2026)**: endpoints `/api/auth/login`, `/api/auth/verify-password` e `/api/auth/change-password` extraídos para `apps/auth-service/src/routes/auth-password-routes.ts`.
- **Rotas de biometria do Auth Service modularizadas (07/03/2026)**: endpoints `/api/auth/biometrics/*` extraídos para `apps/auth-service/src/routes/auth-biometrics-routes.ts`.
- **Rotas de registro administrativo do Auth Service modularizadas (07/03/2026)**: endpoint `/api/auth/register` extraído para `apps/auth-service/src/routes/auth-registration-routes.ts`, mantendo políticas RBAC (`admin`) e provisioning assíncrono.
- **Rotas WebSocket/Status de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/status`, `/api/integrations/trading/ws/status`, `/api/integrations/trading/intervals`, `/api/integrations/trading/ws/subscribe` e `/api/integrations/trading/ws/unsubscribe` extraídos para `apps/integrations-service/src/routes/trading-websocket-routes.ts`.
- **Rotas de símbolos de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/symbols` e `/api/integrations/trading/symbol-preferences` (GET/PUT) extraídos para `apps/integrations-service/src/routes/trading-symbol-routes.ts`, preservando validação, persistência e defaults por mercado.
- **Rotas de mercado/conta/posições/risco de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/market*`, `/account`, `/positions*` e `/risk-config` extraídos para `apps/integrations-service/src/routes/trading-market-risk-routes.ts`.
- **Rotas de account management de Trading modularizadas (07/03/2026)**: endpoints avançados de `/api/integrations/trading/account/*` (summary, API key, ledgers, sub-accounts, deposit/withdraw, transfer e fees) extraídos para `apps/integrations-service/src/routes/trading-account-management-routes.ts`.
- **Rotas de automação de Trading modularizadas (07/03/2026)**: endpoints `/api/trading/portfolios`, `/api/trading/candidates`, `/api/trading/rebalances`, `/internal/trading/enqueue/*` e `/api/trading/auto/*` extraídos para `apps/integrations-service/src/routes/trading-automation-routes.ts`, mantendo idempotência de enqueue e trilha operacional de auto-runs.
- **Rotas de histórico de sinais de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/signals` e `/api/integrations/trading/signals/history*` extraídos para `apps/integrations-service/src/routes/trading-signal-history-routes.ts`, preservando filtros, paginação, stats e governança de soft-delete/purge por escopo.
- **Rotas de ação de sinais de Trading modularizadas (07/03/2026)**: endpoints de ação (`create`, `deactivate`, `approve`, `reject`) em `/api/integrations/trading/signals*` extraídos para `apps/integrations-service/src/routes/trading-signal-action-routes.ts`, preservando fluxo training-only para sinais `neutral/hold` e trilha de auditoria.
- **Rotas de governança de ordens de Trading modularizadas (07/03/2026)**: endpoints de `/api/integrations/trading/orders*`, `/api/integrations/trading/audit/:entityType/:id` e `POST /api/integrations/trading/stop-orders` extraídos para `apps/integrations-service/src/routes/trading-order-governance-routes.ts`, preservando sincronização com dataset e verificação de integridade do audit ledger.
- **Rota de geração de sinais de Trading modularizada (07/03/2026)**: endpoint `POST /api/integrations/trading/signals/generate` extraído para `apps/integrations-service/src/routes/trading-signal-generation-routes.ts`, preservando scan de universo, validação de mercado e governança de erros de escopo (`TRADING_SCOPE_REQUIRED`).
- **Rotas de datasets de Trading modularizadas (07/03/2026)**: endpoints `GET /api/integrations/trading/datasets*`, `POST /api/integrations/trading/datasets/from-signal` e `PATCH /api/integrations/trading/datasets/:id/review` extraídos para `apps/integrations-service/src/routes/trading-dataset-routes.ts`, preservando governança de qualidade, deduplicação e validação de namespace/tenant.
- **Rotas de scheduler e presets de notícias de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/signal-scheduler`, `/api/integrations/trading/analysis-scheduler` e `/api/integrations/trading/news-presets*` extraídos para `apps/integrations-service/src/routes/trading-scheduler-news-routes.ts`, preservando validações de mercado/arbitragem e aplicação de presets no profile.
- **Rotas Futures de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/futures/*` e alias legado `/api/integrations/trading/positions/history` extraídos para `apps/integrations-service/src/routes/trading-futures-routes.ts`, preservando cobertura completa de ordens, posições, índices e funding.
- **Rotas Spot de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/spot/*` extraídos para `apps/integrations-service/src/routes/trading-spot-routes.ts`, preservando cobertura de ordens, OCO, stop orders, fills, DCP e market data avançado.
- **Rotas Margin de Trading modularizadas (07/03/2026)**: endpoints `/api/integrations/trading/margin/*` extraídos para `apps/integrations-service/src/routes/trading-margin-routes.ts`, preservando cobertura de ordens, OCO, borrow/repay, juros, risk-limit e market data avançado.
- **Rotas de controle de Trading modularizadas (07/03/2026)**: endpoints `GET /api/integrations/trading/control-history` e `POST /api/integrations/trading/control` extraídos para `apps/integrations-service/src/routes/trading-control-routes.ts`, preservando handover/takeover, trilha auditável e broadcast de mudanças de controle.
- **Rotas de stop orders de Trading modularizadas (07/03/2026)**: endpoints `GET /api/integrations/trading/stop-orders` e `DELETE /api/integrations/trading/stop-orders/:id` extraídos para `apps/integrations-service/src/routes/trading-stop-order-routes.ts`, preservando validação de mercado/símbolo e guardrails de configuração KuCoin.
- **Rotas de market data de Trading modularizadas (07/03/2026)**: endpoints `klines`, `orderbook`, `funding-rate`, `mark-price` e `trades` foram extraídos para `apps/integrations-service/src/routes/trading-market-data-routes.ts`, mantendo compatibilidade de rotas legadas e hardening de autenticação/configuração KuCoin.
- **Histórico de ordens consolidado no módulo de governança (07/03/2026)**: endpoints `GET /api/integrations/trading/orders/history` e `POST /api/integrations/trading/orders/history/delete` foram removidos do `index.ts` e consolidados em `apps/integrations-service/src/routes/trading-order-governance-routes.ts`, preservando cursor, escopo (`self/tenant`) e soft-delete auditável.
- **Rotas de validação LLM modularizadas no Integrations (07/03/2026)**: endpoints `GET /api/integrations/trading/validations` e `GET /api/integrations/trading/validations/diagnostics` foram extraídos para `apps/integrations-service/src/routes/trading-validation-routes.ts`, preservando agregações SQL e execução RLS-safe com `withTenantContext`.
- **Histórico de análises modularizado no Integrations (07/03/2026)**: endpoints `GET /api/integrations/trading/analysis/history`, `POST /api/integrations/trading/analysis/history/delete` e `POST /api/integrations/trading/analysis/history/purge` foram extraídos para `apps/integrations-service/src/routes/trading-analysis-history-routes.ts`, preservando filtros e governança de exclusão por escopo.
- **Rotas principais de análise modularizadas no Integrations (07/03/2026)**: endpoints `GET/PUT /api/integrations/trading/analysis-profile`, `GET /api/integrations/trading/arbitrage/catalog` e `GET /api/integrations/trading/analysis/:symbol` foram extraídos para `apps/integrations-service/src/routes/trading-analysis-routes.ts`, removendo os últimos handlers inline de análise do `index.ts` e preservando o pipeline determinístico completo.
- **Rotas de plataforma modularizadas no Training (07/03/2026)**: endpoints de health/probes (`/api/training/health`, `/live`, `/ready`), enqueue/auto-run interno de trading (`/internal/trading/enqueue/*`, `/internal/trading/auto/*`) e governança de `system-config` (`GET/PATCH /api/training/system-config`) foram extraídos para `apps/training-service/src/routes/training-platform-routes.ts`.
- **Rotas de auditoria modularizadas no Training (07/03/2026)**: endpoints `GET /api/training/audit/integrity` e `GET /api/training/audit/high-risk` foram extraídos para `apps/training-service/src/routes/training-audit-routes.ts`, preservando validação de tenant/autorização e consultas de governança em trilha imutável.
- **Rotas LoRA/Orchestrator modularizadas no Training (07/03/2026)**: endpoints `/api/training/lora/*` e `/api/training/gpu-orchestrator/*` foram extraídos para `apps/training-service/src/routes/training-lora-orchestrator-routes.ts`, preservando governança de escopo/tenant e contratos de proxy com o GPU Manager.
- **Rotas de runtime modularizadas no Training (07/03/2026)**: endpoints `/api/training/auto-learning/status`, `/api/training/execution-modes`, `/api/training/stats` e `/api/training/queue/status` foram extraídos para `apps/training-service/src/routes/training-runtime-routes.ts`, preservando governança operacional e observabilidade de filas.
- **Rotas de run management modularizadas no Training (07/03/2026)**: endpoints `/api/training/run/status`, `/api/training/run/history` e `/api/training/run/cancel` foram extraídos para `apps/training-service/src/routes/training-run-management-routes.ts`, preservando governança de cancelamento, histórico e visibilidade operacional de runs.
- **Rotas de schedule modularizadas no Training (07/03/2026)**: endpoint `/api/training/schedule/configure` foi extraído para `apps/training-service/src/routes/training-schedule-routes.ts`, preservando reconciliação de escopo e cálculo de próxima execução.
- **Rotas de data review modularizadas no Training (07/03/2026)**: endpoint `/api/training/data/approve-batch` foi extraído para `apps/training-service/src/routes/training-data-review-routes.ts`, preservando governança de quarentena e métricas de revisão.
- **Rotas de bulk import modularizadas no Training (07/03/2026)**: endpoint `/api/training/bulk-import` foi extraído para `apps/training-service/src/routes/training-bulk-import-routes.ts`, preservando validação de escopo, dedupe semântico e enqueue assíncrono de embedding.
- **Rotas de webhook modularizadas no Training (07/03/2026)**: endpoint `/api/training/webhook` foi extraído para `apps/training-service/src/routes/training-webhook-routes.ts`, preservando assinatura/digest/nonce anti-replay e validação de tenant interno.
- **Rotas de dados de treinamento modularizadas no Training (07/03/2026)**: endpoints `POST/GET /api/training/data`, `PATCH /api/training/data/:id/status` e `PATCH /api/training/data/:id/resolve-scope` foram extraídos para `apps/training-service/src/routes/training-data-routes.ts`, preservando governança de escopo, trilha de auditoria e métricas de review/override.
- **Rotas de consulta de jobs modularizadas no Training (07/03/2026)**: endpoints `GET /api/training/jobs`, `GET /api/training/jobs/:id`, `GET /api/training/jobs/:id/stream`, `GET /api/training/jobs/:id/promotion-approvals` e `GET /api/training/jobs/:id/audit-trail` foram extraídos para `apps/training-service/src/routes/training-job-query-routes.ts`, preservando stream SSE, governança de aprovações e trilha imutável por tenant.
- **Rota de cancelamento de jobs modularizada no Training (07/03/2026)**: endpoint `DELETE /api/training/jobs/:id` foi extraído para `apps/training-service/src/routes/training-job-cancel-routes.ts`, preservando contratos de erro, validação de estado terminal e cancelamento governado do LoRA vinculado.
- **Rota de aprovação de promoção modularizada no Training (07/03/2026)**: endpoint `POST /api/training/jobs/:id/promotion-approval` foi extraído para `apps/training-service/src/routes/training-job-promotion-approval-routes.ts`, preservando lock de concorrência, trilha de auditoria e governança de aprovação por tenant.
- **Rota de rollback modularizada no Training (07/03/2026)**: endpoint `POST /api/training/jobs/:id/rollback` foi extraído para `apps/training-service/src/routes/training-job-rollback-routes.ts`, preservando lock de concorrência, validação de escopo e trilha de auditoria de rollback.
- **Rota de promoção modularizada no Training (07/03/2026)**: endpoint `POST /api/training/jobs/:id/promote` foi extraído para `apps/training-service/src/routes/training-job-promote-routes.ts`, preservando gates de avaliação/aprovação, lock por escopo e trilha de auditoria de promoção.
- **Rota de run/start on-demand modularizada no Training (07/03/2026)**: endpoint `POST /api/training/run/start` foi extraído para `apps/training-service/src/routes/training-run-start-routes.ts`, preservando idempotência por chave, lock de concorrência, enqueue assíncrono e auditoria de governança.
- **Rota de criação de jobs modularizada no Training (07/03/2026)**: endpoint `POST /api/training/jobs` foi extraído para `apps/training-service/src/routes/training-job-create-routes.ts`, preservando idempotência por chave, lock de concorrência, seleção de dataset, enqueue assíncrono e auditoria de governança.
- **Serviços de governança modularizados no Training (07/03/2026)**: o `index.ts` passou a consumir módulos dedicados `apps/training-service/src/training-governance-audit.ts` e `apps/training-service/src/training-promotion-approvals.ts`, centralizando catálogo de ações auditáveis, persistência de trilha imutável e resumo de aprovações de promoção.
- **Serviço de lifecycle modularizado no Training (07/03/2026)**: o `index.ts` passou a consumir `apps/training-service/src/training-job-lifecycle.ts`, centralizando retomada de jobs pendentes pós-restart e cancelamento governado de fine-tuning/LoRA.
- **Serviço de idempotência run-start modularizado no Training (07/03/2026)**: o `index.ts` passou a consumir `apps/training-service/src/training-run-start-idempotency.ts`, centralizando validação de header idempotente, fingerprint determinístico, lookup/store em Redis e respostas padronizadas de erro para fluxos de início de treino.
- **Guardrails OpenAPI/RBAC reforçados no Integrations (07/03/2026)**: `openapi-specs.ts` e testes de sincronização passaram a cobrir `signal-scheduler`, `analysis-scheduler` e governança de `datasets` de trading, reduzindo risco de drift entre contrato e runtime.
- **Cobertura contratual crítica ampliada no Integrations (07/03/2026)**: OpenAPI + testes de sincronização agora cobrem também `analysis-profile`, `arbitrage/catalog`, `stop-orders`, `klines/orderbook/funding/mark/trades`, `control/control-history`, `analysis/{symbol}`, `analysis history purge` e `validations/diagnostics`, com verificação de permissão por método quando necessário.
- **Workspace de Chat segmentado por contexto (07/03/2026)**: `apps/frontend-service/src/pages/Chat/index.tsx` passou a separar a experiência em `Conversa`, `Operações`, `Governança` e `Diagnóstico`, mantendo paridade funcional e reduzindo sobrecarga cognitiva no fluxo diário.
- **Hardening de trust interno na governança LLM (07/03/2026)**: mutações em `apps/llm-gateway-service/src/index.ts` para `/api/llm/governance/*` agora exigem ator HMAC com role `admin/super_admin` e bloqueiam `actor mismatch`; política centralizada em `apps/llm-gateway-service/src/governance-auth.ts` com cobertura em `tests/unit/llm-gateway-governance-auth.test.ts`.
- **Client do LLM Gateway com HMAC + trace context (07/03/2026)**: `packages/shared-utils/src/llm/llm-gateway-client.ts` passou a priorizar `generateInternalAuthHeaders` para service-to-service trust com actor assinado, propagar `traceparent`/`x-correlation-id`/`x-request-id` e manter fallback legado por secret só quando não há contexto assinável.
- **Hardening de auth interno no Observability Service (07/03/2026)**: `apps/observability-service/src/index.ts` passou a consolidar validação interna em `res.locals.internalAuthValidated` (HMAC ou secret) e reutilizar essa marca nos guards RBAC de leitura/admin/logs.
- **Gates de promoção ponta a ponta no Training (07/03/2026)**: nova chave de governança `TRAINING_PROMOTION_REQUIRE_APPROVAL_GATES` incorporada ao SSOT de `system_config`, com enforcement em promoção manual (`training-job-promote-routes.ts`) e bloqueio de auto-promoção agendada sem aprovação explícita (`training-runner.ts` registra estado `waiting_approvals`).
- **Guardrails de testes de Training adaptados à arquitetura modular (07/03/2026)**: helper `tests/unit/services/helpers/training-source.ts` criado para compor `index.ts` + `routes/*` + módulos de governança, mantendo sincronização de testes OpenAPI/RBAC/security/tenant-context com a estrutura real pós-decomposição.
- **Tracing distribuído no Observability Service (07/03/2026)**: `apps/observability-service/src/index.ts` passou a usar `createCorrelationMiddleware`, padronizando propagação de `traceparent`, `x-correlation-id` e `x-request-id` no serviço de observabilidade.
- **DR offsite criptografado no Observability Service (07/03/2026)**: `apps/observability-service/src/backup-orchestrator.ts` passou a persistir artefatos por `backupId`, sincronizar cópia offsite criptografada com manifesto de verificação e enriquecer `POST /api/backup/verify/:id` com prontidão operacional de restore (local/offsite + pgBackRest).
- **Padronização de filtros/estados vazios no Frontend (07/03/2026)**: componentes compartilhados `apps/frontend-service/src/components/ui/workspace-filter-bar.tsx` e `apps/frontend-service/src/components/ui/empty-state.tsx` adicionados ao baseline P2, com adoção inicial em `Trading`, `WisePayments`, `Training`, `Chat`, `Documents`, `Agents`, `Namespaces`, `UsersAdmin` e `DemoTrading`.
- **Expansão de empty states P2 (07/03/2026)**: `EmptyState` evoluiu de baseline inicial para cobrir também cenários críticos de `apps/frontend-service/src/pages/Trading.tsx` e `apps/frontend-service/src/pages/UsersAdmin.tsx`, mantendo consistência visual e reduzindo duplicação de estados vazios.
- **Expansão adicional de empty states P2 em Documents (07/03/2026)**: `apps/frontend-service/src/pages/Documents.tsx` passou a consumir `EmptyState` nos cenários vazios de documentos e mídias (com preservação de ícones e textos existentes), reforçando consistência de UX entre workspaces.
- **Padronização de tabelas vazias P2 em UsersAdmin (07/03/2026)**: componente compartilhado `apps/frontend-service/src/components/ui/table-empty-row.tsx` criado para consolidar estados sem dados em tabelas com `colSpan` explícito e adotado nas tabelas de usuários/permissões do `UsersAdmin`.
- **Padronização de vazio em diálogo de UsersAdmin (07/03/2026)**: fluxo de atribuição de grupos no diálogo de usuário passou a usar `EmptyState` quando não há grupos cadastrados, mantendo coerência visual com o padrão P2.
- **Conclusão de aplicação no diálogo de UsersAdmin (07/03/2026)**: implementação efetiva consolidada em `apps/frontend-service/src/pages/UsersAdmin.tsx`, removendo mensagem inline ad-hoc e unificando com o padrão compartilhado de `EmptyState`.
- **Decomposição incremental de UsersAdmin iniciada (07/03/2026)**: aba de usuários foi extraída para `apps/frontend-service/src/pages/users-admin/components/users-tab-content.tsx`, reduzindo o tamanho do bloco inline no `UsersAdmin.tsx` e preparando continuidade da quebra por submódulos (`groups`, `roles`, `permissions`).
- **Decomposição incremental de UsersAdmin avançada (07/03/2026)**: aba de grupos foi extraída para `apps/frontend-service/src/pages/users-admin/components/groups-tab-content.tsx`, mantendo os handlers no container e reduzindo acoplamento estrutural da página.
- **Decomposição incremental de UsersAdmin evoluída (07/03/2026)**: aba de roles foi extraída para `apps/frontend-service/src/pages/users-admin/components/roles-tab-content.tsx`, mantendo navegação para permissões no container e reduzindo acoplamento estrutural da página.
- **Decomposição incremental de UsersAdmin consolidada nas abas principais (07/03/2026)**: aba de permissões foi extraída para `apps/frontend-service/src/pages/users-admin/components/permissions-tab-content.tsx`, preservando fluxo de toggle com debounce/save queue no container e reduzindo acoplamento estrutural da página.
- **Decomposição incremental de dialog do UsersAdmin (07/03/2026)**: diálogo de permissões de role customizada foi extraído para `apps/frontend-service/src/pages/users-admin/components/custom-role-permissions-dialog.tsx`, preservando o fluxo de toggle/debounce assíncrono no container.
- **Decomposição incremental do diálogo principal de usuário no UsersAdmin (07/03/2026)**: seções `profile`, `roles`, `customRoles` e `groups` foram extraídas para `apps/frontend-service/src/pages/users-admin/components/user-dialog-profile-section.tsx`, `user-dialog-roles-section.tsx`, `user-dialog-custom-roles-section.tsx` e `user-dialog-groups-section.tsx`, mantendo o container responsável por estado/mutações e reduzindo acoplamento estrutural.
- **Decomposição incremental de tabs no Documents (07/03/2026)**: conteúdos das tabs `documents` e `media` foram extraídos para `apps/frontend-service/src/pages/documents/components/documents-tab-content.tsx` e `apps/frontend-service/src/pages/documents/components/media-tab-content.tsx`, mantendo `Documents.tsx` como container de estado/mutações/render callbacks.
- **Decomposição incremental de dialogs operacionais no Documents (07/03/2026)**: dialogs de upload, confirmação de exclusão e envio para treinamento foram extraídos para `apps/frontend-service/src/pages/documents/components/upload-dialog.tsx`, `delete-confirm-dialog.tsx` e `media-send-training-dialog.tsx`, mantendo o container principal como orchestrator de estado/mutações.
- **Decomposição incremental do upload zone no Documents (07/03/2026)**: dropzone de upload foi extraído para `apps/frontend-service/src/pages/documents/components/upload-zone.tsx`, removendo lógica inline do container e reduzindo acoplamento estrutural.
- **Decomposição incremental do viewer dialog no Documents (07/03/2026)**: visualizador/edição de documento foi extraído para `apps/frontend-service/src/pages/documents/components/document-viewer-dialog.tsx`, mantendo `Documents.tsx` como orchestrator de estado/mutações e reduzindo acoplamento estrutural.
- **Decomposição incremental de cards no Documents (07/03/2026)**: componentes de apresentação `DocumentCard` e `MediaCard` foram extraídos para `apps/frontend-service/src/pages/documents/components/document-card.tsx` e `apps/frontend-service/src/pages/documents/components/media-card.tsx`, mantendo `Documents.tsx` como container de estado/mutações e reduzindo acoplamento estrutural.
- **Decomposição incremental do workspace header no Documents (07/03/2026)**: cabeçalho operacional de `Documents` (título, métricas, workspace filter e tabs) foi extraído para `apps/frontend-service/src/pages/documents/components/documents-workspace-header.tsx`, mantendo o container focado em estado/mutações.
- **Decomposição incremental de types/config no Documents (07/03/2026)**: contratos de tipos e configuração de workspace/tabs/status foram extraídos para `apps/frontend-service/src/pages/documents/types.ts` e `apps/frontend-service/src/pages/documents/config.ts`, mantendo `Documents.tsx` focado em orquestração de estado/mutações.
- **Decomposição incremental de formulários no UsersAdmin (07/03/2026)**: dialogs `GroupFormDialog`, `CustomRoleFormDialog` e `PermissionFormDialog` foram extraídos para `apps/frontend-service/src/pages/users-admin/components/`, com schemas/helpers centralizados em `apps/frontend-service/src/pages/users-admin/form-schemas.ts` e tipos centralizados em `apps/frontend-service/src/pages/users-admin/types.ts`.
- **Decomposição incremental de gestão de membros no UsersAdmin (07/03/2026)**: diálogo de membros de grupo foi extraído para `apps/frontend-service/src/pages/users-admin/components/group-members-dialog.tsx`, mantendo `UsersAdmin.tsx` como container de estado/mutações e preservando contratos de API/RBAC.
- **Decomposição incremental de orquestração de permissões no UsersAdmin (07/03/2026)**: debounce/save queue de permissões de role e custom role foi extraído para `apps/frontend-service/src/pages/users-admin/hooks/use-role-permission-orchestration.ts`, reduzindo densidade do `UsersAdmin.tsx` sem alteração de contratos.
- **Decomposição incremental de lifecycle de usuário no UsersAdmin (07/03/2026)**: fluxos de criação/edição/salvamento/status foram extraídos para `apps/frontend-service/src/pages/users-admin/hooks/use-user-management.ts`, mantendo `UsersAdmin.tsx` como composition root de tela com menor densidade estrutural.
- **Decomposição incremental de mutações em Documents (07/03/2026)**: mutações de upload, exclusão, reprocessamento e envio para treinamento foram extraídas para `apps/frontend-service/src/pages/documents/hooks/use-documents-mutations.ts`, reduzindo densidade de `Documents.tsx` sem alteração de contratos.
- **Decomposição incremental de orquestração de dialogs em Documents (07/03/2026)**: handlers de abertura/fechamento/confirmação dos dialogs de exclusão e envio para treinamento foram extraídos para `apps/frontend-service/src/pages/documents/hooks/use-documents-dialog-orchestration.ts`, reduzindo acoplamento de estado transiente no container sem alteração de contratos.
- **Decomposição incremental de estado derivado/filtros em Documents (07/03/2026)**: filtros, stats, namespace map e listas derivadas de documentos/mídias foram extraídos para `apps/frontend-service/src/pages/documents/hooks/use-documents-derived-state.ts`, reduzindo densidade lógica de `Documents.tsx` sem alteração de contratos.
- **Decomposição incremental da aba de ordens em Trading (07/03/2026)**: conteúdo da aba `orders` foi extraído para `apps/frontend-service/src/components/trading/TradingOrdersTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da aba portfolio-auto em Trading (07/03/2026)**: conteúdo da aba `portfolio-auto` foi extraído para `apps/frontend-service/src/components/trading/TradingPortfolioAutoTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da aba signals-auto em Trading (07/03/2026)**: conteúdo da aba `signals-auto` foi extraído para `apps/frontend-service/src/components/trading/TradingSignalsAutoTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da aba lab em Trading (07/03/2026)**: conteúdo da aba `lab` foi extraído para `apps/frontend-service/src/components/trading/TradingLabTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental das abas control/account em Trading (07/03/2026)**: conteúdos das abas `control` e `account` foram extraídos para `apps/frontend-service/src/components/trading/TradingControlTabContent.tsx` e `apps/frontend-service/src/components/trading/TradingAccountTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da aba positions em Trading (07/03/2026)**: conteúdo da aba `positions` foi extraído para `apps/frontend-service/src/components/trading/TradingPositionsTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental das abas history/postmortems em Trading (07/03/2026)**: conteúdos das abas `history` e `postmortems` foram extraídos para `apps/frontend-service/src/components/trading/TradingHistoryTabContent.tsx` e `apps/frontend-service/src/components/trading/TradingPostMortemsTabContent.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da seção de resultados de signals em Trading (07/03/2026)**: bloco de detalhe/lista/aprovação da aba `signals` foi extraído para `apps/frontend-service/src/components/trading/TradingSignalsResultsSection.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da seção de scheduler de signals em Trading (07/03/2026)**: bloco de configuração/status/salvamento do scheduler da aba `signals` foi extraído para `apps/frontend-service/src/components/trading/TradingSignalsSchedulerSection.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da seção de configuração de perfil de signals em Trading (07/03/2026)**: bloco de timeframes/indicadores/técnicas/ensemble/arbitragem/fontes da aba `signals` foi extraído para `apps/frontend-service/src/components/trading/TradingSignalsProfileConfigurationSection.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental da seção de news/actions de signals em Trading (07/03/2026)**: bloco de `NewsConfigEditor` e ações operacionais (`save profile`, `generate now`, `create/update preset`) da aba `signals` foi extraído para `apps/frontend-service/src/components/trading/TradingSignalsNewsAndActionsSection.tsx`, mantendo `Trading.tsx` como composition root e reduzindo acoplamento visual/operacional sem alteração de contratos.
- **Decomposição incremental do diálogo de criação de signals em Trading (07/03/2026)**: diálogo de novo sinal da aba `signals` foi extraído para `apps/frontend-service/src/components/trading/TradingNewSignalDialog.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo acoplamento visual sem alteração de contratos.
- **Decomposição incremental do diálogo de envio de post-mortem em Trading (07/03/2026)**: diálogo de envio para treinamento foi extraído para `apps/frontend-service/src/components/trading/TradingPostmortemTrainingDialog.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo acoplamento visual sem alteração de contratos.
- **Decomposição incremental do diálogo de revisão de ordem em Trading (07/03/2026)**: diálogo de revisão/aprovação de ordens pendentes foi extraído para `apps/frontend-service/src/components/trading/TradingReviewOrderDialog.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo acoplamento visual sem alteração de contratos.
- **Decomposição incremental do diálogo de configuração de risco em Trading (07/03/2026)**: diálogo de limites/defaults de risco foi extraído para `apps/frontend-service/src/components/trading/TradingRiskConfigDialog.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo acoplamento visual sem alteração de contratos.
- **Decomposição incremental do diálogo de nova ordem em Trading (07/03/2026)**: diálogo operacional de criação de ordens (resumo, conversão contratos/USDT, leverage e SL/TP) foi extraído para `apps/frontend-service/src/components/trading/TradingNewOrderDialog.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo acoplamento visual sem alteração de contratos.
- **Decomposição incremental das abas analysis/chart/orderbook em Trading (08/03/2026)**: os blocos inline dessas abas foram extraídos para `apps/frontend-service/src/components/trading/TradingAnalysisTabContent.tsx`, `TradingChartTabContent.tsx` e `TradingOrderBookTabContent.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo densidade residual sem alteração de contratos.
- **Decomposição incremental da aba overview em Trading (08/03/2026)**: o bloco operacional da aba `overview` foi extraído para `apps/frontend-service/src/components/trading/TradingOverviewTabContent.tsx`, mantendo `Trading.tsx` como composition root de estado/mutações e reduzindo densidade residual sem alteração de contratos.
- **Decomposição incremental das linhas de métricas em Trading (08/03/2026)**: os cards de métricas de mercado/conta e status operacional foram extraídos para `apps/frontend-service/src/components/trading/TradingStatsRows.tsx` (`TradingStatsPrimaryRow` e `TradingStatsSecondaryRow`), reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental do header operacional em Trading (08/03/2026)**: o bloco de título/status, seletores de mercado/símbolo, ações de favoritos/destaques, indicador WS e acesso a risco foi extraído para `apps/frontend-service/src/components/trading/TradingHeaderSection.tsx`, reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental de alertas operacionais em Trading (08/03/2026)**: os alertas de erro crítico de upstream e trading desabilitado foram extraídos para `apps/frontend-service/src/components/trading/TradingOperationalAlerts.tsx`, reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental do shell de tabs em Trading (08/03/2026)**: a estrutura compartilhada de navegação (`Tabs + WorkspaceFilterBar + TabsList/TabsTrigger`) foi extraída para `apps/frontend-service/src/components/trading/TradingTabsShell.tsx`, reduzindo acoplamento de UI no `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental da aba signals em Trading (08/03/2026)**: o bloco operacional de sinais (`perfil + news/actions + scheduler + resultados`) foi extraído para `apps/frontend-service/src/components/trading/TradingSignalsTabContent.tsx`, reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental da seção de dialogs em Trading (08/03/2026)**: o bloco de dialogs operacionais (`nova ordem`, `OCO`, `review`, `risk config`, `post-mortem->training`, `novo sinal`) foi extraído para `apps/frontend-service/src/components/trading/TradingDialogsSection.tsx`, reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental das abas operacionais residuais em Trading (08/03/2026)**: as abas `history`, `postmortems`, `chart`, `orderbook`, `control` e `account` foram agrupadas em `apps/frontend-service/src/components/trading/TradingOperationalTabsSection.tsx`, reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental das abas primárias em Trading (08/03/2026)**: as abas `overview`, `portfolio-auto`, `signals-auto`, `lab`, `orders`, `positions`, `signals` e `analysis` foram agrupadas em `apps/frontend-service/src/components/trading/TradingPrimaryTabsSection.tsx`, reduzindo densidade residual do `Trading.tsx` sem alteração de contratos.
- **Decomposição incremental da aba balances em WisePayments (08/03/2026)**: a aba `balances` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-balances-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba exchange em WisePayments (08/03/2026)**: a aba `exchange` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-exchange-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba transfers em WisePayments (08/03/2026)**: a aba `transfers` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-transfers-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba recipients em WisePayments (08/03/2026)**: a aba `recipients` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-recipients-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba quotes em WisePayments (08/03/2026)**: a aba `quotes` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-quotes-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba batch em WisePayments (08/03/2026)**: a aba `batch` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-batch-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba profiles em WisePayments (08/03/2026)**: a aba `profiles` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-profiles-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba users em WisePayments (08/03/2026)**: a aba `users` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-users-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba activities em WisePayments (08/03/2026)**: a aba `activities` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-activities-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba statements em WisePayments (08/03/2026)**: a aba `statements` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-statements-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba account-details em WisePayments (08/03/2026)**: a aba `account-details` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-account-details-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba cards em WisePayments (08/03/2026)**: a aba `cards` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-cards-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba card-orders em WisePayments (08/03/2026)**: a aba `card-orders` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-card-orders-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba card-transactions em WisePayments (08/03/2026)**: a aba `card-transactions` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-card-transactions-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba spend-limits em WisePayments (08/03/2026)**: a aba `spend-limits` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-spend-limits-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba spend-controls em WisePayments (08/03/2026)**: a aba `spend-controls` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-spend-controls-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba disputes em WisePayments (08/03/2026)**: a aba `disputes` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-disputes-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba kyc em WisePayments (08/03/2026)**: a aba `kyc` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-kyc-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba webhooks em WisePayments (08/03/2026)**: a aba `webhooks` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-webhooks-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba simulations em WisePayments (08/03/2026)**: a aba `simulations` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-simulations-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba sca em WisePayments (08/03/2026)**: a aba `sca` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-sca-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da aba catalog em WisePayments (08/03/2026)**: a aba `catalog` foi extraída para `apps/frontend-service/src/pages/wise-payments/components/wise-catalog-tab-content.tsx`, reduzindo acoplamento da mega-página `WisePayments.tsx` sem alteração de contratos.
- **Decomposição incremental da navegação/workspaces em WisePayments (08/03/2026)**: catálogo de tabs, mapeamento de workspaces e tipos (`WiseTabKey`/`WiseWorkspaceKey`) foram extraídos para `apps/frontend-service/src/pages/wise-payments/wise-payments-navigation.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental do guard de queries em WisePayments (08/03/2026)**: lógica de bloqueio temporário após `401/429` e tratamento centralizado de erro foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-query-guard.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental do refresh/dados derivados em WisePayments (08/03/2026)**: composição residual de `refetch` e mapeamento tipado de coleções (`balances`, `transfers`, `cards`, `spendControls`, `disputes`, `kyc`, etc.) foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-refresh-actions.ts` e `apps/frontend-service/src/pages/wise-payments/use-wise-derived-data.ts`, reduzindo lógica inline no container `WisePayments.tsx`.
- **Decomposição incremental de payload/review em Trading (08/03/2026)**: builder de payload de perfil de sinais e validação de completude foram extraídos para `apps/frontend-service/src/components/trading/TradingSignalProfilePayload.ts`, e handlers residuais de revisão de ordem foram extraídos para `apps/frontend-service/src/components/trading/useTradingReviewOrderHandlers.ts`, reduzindo lógica inline no container `Trading.tsx`.
- **Decomposição incremental de post-mortem training handlers em Trading (08/03/2026)**: fluxo de `open/cancel/openChange/submit` do envio de post-mortem para treinamento foi extraído para `apps/frontend-service/src/components/trading/useTradingPostmortemTrainingHandlers.ts`, removendo duplicação de handlers inline no container `Trading.tsx`.
- **Decomposição incremental de dialogs/forms em Trading (08/03/2026)**: handlers de `open/close/patch/submit` de nova ordem, risco, novo sinal e quick-order foram extraídos para `apps/frontend-service/src/components/trading/useTradingDialogFormHandlers.ts`, removendo callbacks inline repetidos no container `Trading.tsx`.
- **Decomposição incremental de scheduler handlers em Trading (08/03/2026)**: handlers de atualização de scheduler (`enabled`, `intervalMinutes`, `maxSignalsPerRun`, `symbols`) foram extraídos para `apps/frontend-service/src/components/trading/useTradingSchedulerFormHandlers.ts`, removendo callbacks inline residuais no container `Trading.tsx`.
- **Decomposição incremental de mutation action handlers em Trading (08/03/2026)**: wrappers de ações recorrentes de mutação/navegação (`approve`, `cancel`, `reject`, `sync`, `deactivate`, `open generated signal`, `open analysis/lab/signals`) foram extraídos para `apps/frontend-service/src/components/trading/useTradingMutationActionHandlers.ts`, removendo callbacks inline repetidos no container `Trading.tsx`.
- **Decomposição incremental de signal profile action handlers em Trading (08/03/2026)**: handlers de configuração de sinais/presets (`apply/change/create/delete/update preset`, `generate`, `save profile`, `save scheduler`, `ensemble topN`) foram extraídos para `apps/frontend-service/src/components/trading/useTradingSignalProfileActionHandlers.ts`, removendo callbacks inline de maior impacto em `signalsTabProps` no container `Trading.tsx`.
- **Decomposição incremental do filtro de activities em WisePayments (08/03/2026)**: atualização de filtros foi extraída para `handleActivityFilterChange` em `apps/frontend-service/src/pages/wise-payments/use-wise-user-activity-actions.ts`, removendo callback inline residual no container `WisePayments.tsx`.
- **Decomposição incremental da moeda padrão de spend-control em WisePayments (08/03/2026)**: sincronização de `currency` padrão (a partir do primeiro saldo disponível) foi extraída para `apps/frontend-service/src/pages/wise-payments/use-wise-spend-control-default-currency.ts`, removendo `useEffect` inline residual no container `WisePayments.tsx`.
- **Decomposição incremental dos handlers de referência em WisePayments (08/03/2026)**: estados e handlers de `balanceCapacity`, `totalFunds`, `rates` e `recipientRequirements` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-reference-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos handlers de transferência/cartões em WisePayments (08/03/2026)**: estados e handlers de `fund/cancel transfer`, permissões de cartão e fluxos `card secure` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-transfer-and-card-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental de upload de arquivos em WisePayments (08/03/2026)**: estado e handlers de upload para disputas/KYC (`dispute`, `kyc document`, `kyc additional`) foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-file-upload-state.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental do catalog workbench em WisePayments (08/03/2026)**: estado, efeitos de sincronização e handler de execução de operações de catálogo foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-catalog-workbench.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos fluxos webhooks/simulations/sca em WisePayments (08/03/2026)**: estado e mutações operacionais de `webhooks`, `simulations` e `sca` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-webhook-simulation-sca-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos fluxos account-details/card-orders/disputes em WisePayments (08/03/2026)**: estado, mutações e handlers operacionais de `account-details`, `card-orders`, `card-transactions`, `disputes` e `kyc` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-account-card-dispute-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos fluxos users/activities em WisePayments (08/03/2026)**: estado, mutações e handlers operacionais de `users` e `activities` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-user-activity-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos fluxos balances/quotes/exchange/statements em WisePayments (08/03/2026)**: estado, mutações e handlers operacionais de `balances`, `quotes`, `exchange` e `statements` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-balance-exchange-statement-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos fluxos cards/spend-controls/spend-limits em WisePayments (08/03/2026)**: estado, mutações e handlers operacionais de `cards`, `spend-controls` e `spend-limits` foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-card-spend-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental dos fluxos recipients em WisePayments (08/03/2026)**: estado/transições de diálogo e deleção de recipient foram extraídos para `apps/frontend-service/src/pages/wise-payments/use-wise-recipient-actions.ts`, reduzindo densidade do container `WisePayments.tsx`.
- **Decomposição incremental da aba jobs em Training (08/03/2026)**: a aba `jobs` foi extraída para `apps/frontend-service/src/pages/training/components/training-jobs-tab-content.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental da aba auto-learning em Training (08/03/2026)**: a aba `auto-learning` foi extraída para `apps/frontend-service/src/pages/training/components/training-auto-learning-tab-content.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental da aba data em Training (08/03/2026)**: a aba `data` foi extraída para `apps/frontend-service/src/pages/training/components/training-data-tab-content.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental da aba bulk-import em Training (08/03/2026)**: a aba `bulk-import` foi extraída para `apps/frontend-service/src/pages/training/components/training-bulk-import-tab-content.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental da aba multimodal em Training (08/03/2026)**: a aba `multimodal` foi extraída para `apps/frontend-service/src/pages/training/components/training-multimodal-tab-content.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo on-demand em Training (08/03/2026)**: o diálogo de execução manual foi extraído para `apps/frontend-service/src/pages/training/components/training-on-demand-run-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo batch review em Training (08/03/2026)**: o diálogo de aprovação/rejeição em lote foi extraído para `apps/frontend-service/src/pages/training/components/training-batch-review-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo review em Training (08/03/2026)**: o diálogo de aprovação/rejeição individual foi extraído para `apps/frontend-service/src/pages/training/components/training-review-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo resolve scope em Training (08/03/2026)**: o diálogo de relink/resolução de escopo foi extraído para `apps/frontend-service/src/pages/training/components/training-resolve-scope-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo promote em Training (08/03/2026)**: o diálogo de promoção foi extraído para `apps/frontend-service/src/pages/training/components/training-promote-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo rollback em Training (08/03/2026)**: o diálogo de rollback foi extraído para `apps/frontend-service/src/pages/training/components/training-rollback-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do diálogo post-training em Training (08/03/2026)**: o diálogo pós-treinamento foi extraído para `apps/frontend-service/src/pages/training/components/training-post-training-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do componente TrainingDataCard em Training (08/03/2026)**: o card de dataset foi extraído para `apps/frontend-service/src/pages/training/components/training-data-card.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do componente TrainingJobCard em Training (08/03/2026)**: o card de job foi extraído para `apps/frontend-service/src/pages/training/components/training-job-card.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do componente TrainingJobDetailModal em Training (08/03/2026)**: o modal de detalhe de job foi extraído para `apps/frontend-service/src/pages/training/components/training-job-detail-modal.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Decomposição incremental do componente TrainingCreateJobDialog em Training (08/03/2026)**: o diálogo de criação de job foi extraído para `apps/frontend-service/src/pages/training/components/training-create-job-dialog.tsx`, reduzindo acoplamento da mega-página `Training.tsx` sem alteração de contratos.
- **Centralização de utilitários de requisição de Training (08/03/2026)**: funções de idempotência e `retry-after` foram centralizadas em `apps/frontend-service/src/pages/training/training-request-utils.ts`, reduzindo duplicação no container `Training.tsx`.
- **Decomposição incremental dos utilitários de exibição em Trading (08/03/2026)**: badges e formatadores (`SIGNAL_TYPES`, `SignalTypeBadge`, `OrderStatusBadge`, `formatDecisionSummary`) foram extraídos para `apps/frontend-service/src/components/trading/TradingDisplayUtils.tsx` e reexportados em `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental da configuração de sinais em Trading (08/03/2026)**: catálogos e defaults de sinais/automação foram extraídos para `apps/frontend-service/src/components/trading/TradingSignalConfig.ts` e reexportados em `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental da navegação/workspaces em Trading (08/03/2026)**: tipos e catálogos de navegação/workspaces foram extraídos para `apps/frontend-service/src/components/trading/TradingNavigationConfig.ts` e reexportados em `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental dos utilitários de página em Trading (08/03/2026)**: helpers de parsing de símbolo e formatação de duração foram extraídos para `apps/frontend-service/src/components/trading/TradingPageUtils.ts` e reexportados em `apps/frontend-service/src/components/trading/index.ts`.
- **Decomposição incremental dos contratos de domínio em Trading (08/03/2026)**: tipos de payload/conta/sinal/ordem, guards de margem e presets de animação foram extraídos para `apps/frontend-service/src/components/trading/TradingDomainTypes.ts` e reexportados em `apps/frontend-service/src/components/trading/index.ts`, reduzindo densidade do container `Trading.tsx`.
- **Decomposição incremental dos defaults de formulários em Trading (08/03/2026)**: factories tipadas de inicialização/reset foram extraídas para `apps/frontend-service/src/components/trading/TradingFormDefaults.ts` e aplicadas no `Trading.tsx`, reduzindo duplicação de estado e acoplamento do container.
- **Decomposição incremental do hook de perfil de sinais em Trading (08/03/2026)**: estado/updaters/reconciliação de arbitragem do `signalProfile` foram extraídos para `apps/frontend-service/src/components/trading/useTradingSignalProfileState.ts` e aplicados no `Trading.tsx`, reduzindo acoplamento do container.
- **Decomposição incremental do hook de sizing de ordens em Trading (08/03/2026)**: cálculo de preço corrente/`contractMultiplier` e handlers de conversão (`size` <-> `usdtAmount`) foram extraídos para `apps/frontend-service/src/components/trading/useTradingOrderSizing.ts` e aplicados no `Trading.tsx`, reduzindo densidade do container.
- **Decomposição incremental dos cálculos de resumo de ordem em Trading (08/03/2026)**: validação de submissão e estimativas de PnL (preço efetivo, leverage, SL/TP e `canSubmitOrder`) foram extraídas para `apps/frontend-service/src/components/trading/TradingOrderSummary.ts` e aplicadas no `Trading.tsx`, reduzindo densidade do container.
- **Decomposição incremental do hook de presets de notícias em Trading (08/03/2026)**: query/mutações e regras de seleção/criação/atualização/remoção de presets foram extraídas para `apps/frontend-service/src/components/trading/useTradingNewsPresets.ts` e aplicadas no `Trading.tsx`, reduzindo densidade do container.
- **Decomposição incremental do hook de histórico de ordens em Trading (08/03/2026)**: estado, paginação, seleção em lote e exclusão de histórico foram extraídos para `apps/frontend-service/src/components/trading/useTradingOrderHistory.ts` e aplicados no `Trading.tsx`, reduzindo densidade do container.
- **Decomposição incremental da navegação de workspaces/tabs em Trading (08/03/2026)**: estado e handlers (`activeTab`, `activeWorkspace`, troca de tabs/workspaces e reconciliação automática) foram extraídos para `apps/frontend-service/src/components/trading/useTradingWorkspaceNavigation.ts` e aplicados no `Trading.tsx`, reduzindo densidade do container.
- **Decomposição incremental da normalização de mensagens no Chat (08/03/2026)**: normalização de payload do servidor, mapeamento de anexos legados e snapshot de usuário foram extraídos para `apps/frontend-service/src/pages/Chat/chat-message-normalization.ts` e aplicados no `Chat/index.tsx`, reduzindo densidade do container.
- **Decomposição incremental da lista de conversas no Chat (08/03/2026)**: o componente `ConversationsList` foi extraído para `apps/frontend-service/src/pages/Chat/components/ConversationsList.tsx` e aplicado no `Chat/index.tsx`, reduzindo densidade do container.
- **Decomposição incremental do hook mobile no Chat (08/03/2026)**: detecção de viewport mobile foi extraída para `apps/frontend-service/src/pages/Chat/useIsMobileViewport.ts` e aplicada no `Chat/index.tsx`, reduzindo densidade do container.
- **Decomposição incremental dos hooks de auto-scroll e seleção no Chat (08/03/2026)**: scroll automático e seleção em lote/range foram extraídos para `apps/frontend-service/src/pages/Chat/useChatAutoScroll.ts` e `apps/frontend-service/src/pages/Chat/useChatSelectionState.ts`, reduzindo densidade do container `Chat/index.tsx`.
- **Decomposição incremental dos utilitários de rota/sources em Chat (08/03/2026)**: roteamento/workspaces/date filters e parsing de `message sources` foram extraídos para `apps/frontend-service/src/pages/Chat/chat-page-routing.ts` e `apps/frontend-service/src/pages/Chat/chat-message-sources.ts`, reduzindo densidade do container `Chat/index.tsx`.
- **Decomposição incremental dos utilitários de gravação em Chat (08/03/2026)**: normalização de MIME, encode WAV, conversão e preparo de arquivo de gravação foram extraídos para `apps/frontend-service/src/pages/Chat/chat-recording-utils.ts` e aplicados no `Chat/index.tsx`, reduzindo densidade do container.
- **Decomposição incremental dos utilitários de anexos de mídia em Chat (08/03/2026)**: funções de conversão base64 de arquivo/upload/URL (`fileToBase64` e `mediaAttachmentToBase64`) foram extraídas para `apps/frontend-service/src/pages/Chat/chat-media-attachments.ts` e aplicadas no `Chat/index.tsx`, reduzindo densidade do container.
- **Decomposição incremental de ações residuais de workspace em Trading (08/03/2026)**: handlers de refresh/execução/abertura e mutações de histórico (`OCO`, `positions`, `signals-auto`, `history`, `postmortems`, `risk/review dialogs`) foram extraídos para `apps/frontend-service/src/components/trading/useTradingWorkspaceActionHandlers.ts` e aplicados em `Trading.tsx`, reduzindo callbacks inline residuais.
- **Decomposição incremental de refresh handlers em WisePayments (08/03/2026)**: `use-wise-refresh-actions.ts` foi expandido com handlers dedicados de refetch (`account-details`, `profiles`, `users`, `cards`, `card-orders`, `spend-controls`, `disputes`, `kyc`) e `WisePayments.tsx` removeu wrappers inline equivalentes.
- **Decomposição incremental dos estados de acesso do wrapper de Trading (08/03/2026)**: `loading/auth required/forbidden` foram extraídos para `apps/frontend-service/src/components/trading/TradingAccessStates.tsx`, com consumo no wrapper de `apps/frontend-service/src/pages/Trading.tsx` para eliminar markup duplicado sem alterar contratos de API.
- **Decomposição incremental do shell/status de WisePayments (08/03/2026)**: shell de navegação (`Tabs + WorkspaceFilterBar`) foi extraído para `apps/frontend-service/src/pages/wise-payments/components/wise-payments-tabs-shell.tsx` e estados de serviço (`loading/not configured`) para `apps/frontend-service/src/pages/wise-payments/components/wise-payments-status-states.tsx`, reduzindo densidade do container `WisePayments.tsx` sem alterar contratos de API.
- **Decomposição incremental dos estados de serviço de Trading (08/03/2026)**: estados de `loading/error/unavailable/not configured/tenant required` foram extraídos para `apps/frontend-service/src/components/trading/TradingServiceStates.tsx` e integrados ao `apps/frontend-service/src/pages/Trading.tsx`, reduzindo densidade do container sem alterar contratos de API.
- **Decomposição incremental de métricas derivadas de Trading (08/03/2026)**: cálculos de contagem de posições abertas, resumos de conta e variação de preço foram extraídos para `apps/frontend-service/src/components/trading/TradingDerivedMetrics.ts` e integrados ao `apps/frontend-service/src/pages/Trading.tsx`, reduzindo lógica inline sem alterar contratos de API.
- **Decomposição incremental da composição de props em Trading (09/03/2026)**: `apps/frontend-service/src/pages/Trading.tsx` passou a centralizar `props` de tabs/dialogs em objetos nomeados (`primaryTabsSectionProps`, `operationalTabsSectionProps`, `dialogsSectionProps`) consumidos por spread tipado nas seções `TradingPrimaryTabsSection`, `TradingOperationalTabsSection` e `TradingDialogsSection`, reduzindo densidade do trecho de render sem alterar contratos de API.
- **Decomposição incremental da orquestração de mutações controle/ordens em Trading (09/03/2026)**: novo hook de composição `apps/frontend-service/src/components/trading/useTradingControlOrderActionSuite.ts` centralizou a integração entre `useTradingControlOrderMutations`, `useTradingReviewOrderHandlers` e `useTradingMutationActionHandlers`; `apps/frontend-service/src/pages/Trading.tsx` passou a consumir esse boundary único e `apps/frontend-service/src/components/trading/index.ts` foi atualizado com reexport dedicado.
- **Decomposição incremental de navegação e filtros compartilhados em WisePayments (09/03/2026)**: `apps/frontend-service/src/pages/wise-payments/use-wise-navigation-presentation.ts` centralizou `wiseWorkspaceOptions`, `wiseTabOptions` e `handleWiseWorkspaceSelectionChange`; `apps/frontend-service/src/pages/WisePayments.tsx` passou a compartilhar `profileFilter/profiles/setProfileFilter` via `profileScopedTabProps` nas tabs de escopo de perfil.
- **Decomposição incremental da composição de props operacionais em WisePayments (09/03/2026)**: `apps/frontend-service/src/pages/WisePayments.tsx` passou a centralizar props de tabs de perfil em objetos nomeados (`accountDetailsTabProps`, `cardsTabProps`, `cardOrdersTabProps`, `cardTransactionsTabProps`, `spendControlsTabProps`, `disputesTabProps`, `kycTabProps`, `webhooksTabProps`, `simulationsTabProps`, `scaTabProps`) consumidos por spread tipado, reduzindo densidade do trecho de render.
- **Decomposição incremental de menu de ações e handlers compartilhados em Chat (09/03/2026)**: novo componente `apps/frontend-service/src/pages/Chat/components/ChatActionsMenu.tsx` unificou o dropdown de ações para desktop/mobile; `apps/frontend-service/src/pages/Chat/index.tsx` passou a compartilhar `conversationsListProps` entre sidebar/drawer e a usar callbacks nomeados para quick-reply/toggles de seleção e diagnóstico.
- **Decomposição incremental da composição de props residuais em WisePayments (09/03/2026)**: `apps/frontend-service/src/pages/WisePayments.tsx` passou a centralizar também os props das tabs operacionais não-perfil (`balances`, `exchange`, `transfers`, `recipients`, `quotes`, `batch`, `statements`, `profiles`, `users`, `activities`, `spend-limits`, `catalog`) em objetos nomeados consumidos por spread tipado.
- **Decomposição incremental da composição de seções residuais em Trading (09/03/2026)**: `apps/frontend-service/src/pages/Trading.tsx` passou a centralizar props de `TradingOperationalAlerts`, `TradingHeaderSection`, `TradingStatsPrimaryRow`, `TradingStatsSecondaryRow` e `TradingTabsShell` em objetos nomeados (`operationalAlertsSectionProps`, `headerSectionProps`, `statsPrimarySectionProps`, `statsSecondarySectionProps`, `tabsShellSectionProps`) e a reutilizar `renderOrderStatusBadge`/`renderSignalTypeBadge` compartilhados entre tabs.
- **Decomposição incremental dos controles de governança em Chat (09/03/2026)**: `apps/frontend-service/src/pages/Chat/components/ChatGovernanceControls.tsx` centralizou approval policy + routing controls para desktop/mobile; `apps/frontend-service/src/pages/Chat/index.tsx` passou a compartilhar `chatGovernanceControlsProps` entre os dois layouts.
- **Decomposição incremental de memoizações de opções em Trading (09/03/2026)**: `apps/frontend-service/src/pages/Trading.tsx` passou a centralizar mapeamentos de opções em memoizações (`autoModeOptions`, `signalIndicatorOptions`, `signalIntervalOptions`, `signalTechniqueOptions`), removendo mapeamentos inline residuais do `signalsAutoTabProps` e `signalsTabProps`.
- **Decomposição incremental da composição por aba/seção em Trading (09/03/2026)**: `apps/frontend-service/src/pages/Trading.tsx` passou a separar props nomeados por aba/seção antes da montagem de `primaryTabsSectionProps`, `operationalTabsSectionProps` e `dialogsSectionProps` (ex.: `analysisTabProps`, `ordersTabProps`, `signalsTabProps`, `chartTabProps`, `historyTabProps`, `newOrderDialogProps`), reduzindo densidade no composition root.
- **Serviços de canal desacoplados (06/03/2026)**: `apps/integrations-service/src/twilio-channel-service.ts` centraliza assinatura/webhook signing e envio WhatsApp para reuso entre módulos.
- **Rotas OAuth Wise modularizadas (06/03/2026)**: troca/refresh de token e status Wise foram extraídos para `apps/integrations-service/src/routes/wise-oauth-routes.ts`, mantendo o `index.ts` como composição.
- **Webhook Wise modularizado (06/03/2026)**: rota `/api/integrations/wise/webhook` extraída para `apps/integrations-service/src/routes/wise-webhook-routes.ts` com preservação de validação de assinatura, idempotência e auditoria.
- **Rotas de referência Wise modularizadas (07/03/2026)**: endpoints de leitura operacional (`recipient-requirements`, `profiles`, `users`, `activities`) extraídos para `apps/integrations-service/src/routes/wise-reference-routes.ts`.
- **Rotas de account details Wise modularizadas (07/03/2026)**: endpoints `GET/POST /api/integrations/wise/account-details*` extraídos para `apps/integrations-service/src/routes/wise-account-details-routes.ts`.
- **Rotas de saldos/cotações Wise modularizadas (07/03/2026)**: endpoints `/wise/balances*`, `/wise/rates`, `/wise/quotes`, `/wise/balance-quotes`, `/wise/balance-movements`, `/wise/balance-capacity` e `/wise/total-funds` extraídos para `apps/integrations-service/src/routes/wise-balance-and-quotes-routes.ts`.
- **Rotas de recipients/transfers Wise modularizadas (07/03/2026)**: endpoints `/wise/recipients*`, `/wise/transfers*` e `/wise/batch-groups*` extraídos para `apps/integrations-service/src/routes/wise-recipients-transfers-routes.ts`.
- **Rotas de gestão de cartões Wise modularizadas (07/03/2026)**: endpoints de cartões (`/wise/cards*` e permissões em lote) extraídos para `apps/integrations-service/src/routes/wise-card-management-routes.ts`.
- **Rotas de cartões sensíveis/transações Wise modularizadas (07/03/2026)**: endpoints `/wise/cards/transactions/*` e `/wise/cards/secure/*` extraídos para `apps/integrations-service/src/routes/wise-card-secure-routes.ts`.
- **Rotas de card orders Wise modularizadas (07/03/2026)**: endpoints `/wise/card-orders*` extraídos para `apps/integrations-service/src/routes/wise-card-orders-routes.ts`.
- **Rotas de spend controls Wise modularizadas (07/03/2026)**: endpoints `/wise/spend-controls*` extraídos para `apps/integrations-service/src/routes/wise-spend-controls-routes.ts`.
- **Rotas de spend limits Wise modularizadas (07/03/2026)**: endpoints `/wise/spend-limits*` extraídos para `apps/integrations-service/src/routes/wise-spend-limits-routes.ts`.
- **Rotas de disputas Wise modularizadas (07/03/2026)**: endpoints `/wise/disputes*` e fluxo/upload de disputas extraídos para `apps/integrations-service/src/routes/wise-disputes-routes.ts`.
- **Rotas de verificação/KYC Wise modularizadas (07/03/2026)**: endpoints `/wise/verification*` e `/wise/kyc-reviews*` extraídos para `apps/integrations-service/src/routes/wise-verification-kyc-routes.ts`.
- **Rotas SCA Wise modularizadas (07/03/2026)**: endpoints `/wise/one-time-token` e `/wise/sca/*` extraídos para `apps/integrations-service/src/routes/wise-sca-routes.ts`.
- **Gestão de webhooks Wise modularizada (07/03/2026)**: endpoints `/wise/webhooks*` extraídos para `apps/integrations-service/src/routes/wise-webhook-management-routes.ts`.
- **Rotas de simulação Wise modularizadas (07/03/2026)**: endpoints `/wise/simulation/*` extraídos para `apps/integrations-service/src/routes/wise-simulation-routes.ts`.
- **Higienização de imports no integrations-service (07/03/2026)**: imports de módulos de rota e componentes auxiliares foram centralizados no topo do `index.ts` para legibilidade e governança de composition root.
- **Revisão documental transversal (06/03/2026)**: README e documentos SSOT (`ARQUITETURA`, `STATUS-REAL-ATUAL`, `SISTEMA-APRENDIZADO` e `PLANO-IMPLEMENTACAO-ENTERPRISE-BLOCOS`) revisados para remover inconsistências de versão/status e refletir os blocos já implementados.

---

## Regras de atualização (obrigatórias)

- Toda documentação deve estar em **Português Brasileiro** (termos técnicos em inglês).
- Sempre manter **Autor** e **Data** atualizados em cada documento alterado.
- Evitar duplicidade: se um conteúdo já existir em um SSOT, referenciar ao invés de repetir.
- Documentos de **relatório histórico** mantêm contexto temporal original; quando houver divergência de line numbers após refactors, considerar `README.md`, `docs/ARQUITETURA.md` e `docs/STATUS-REAL-ATUAL.md` como referência vigente.

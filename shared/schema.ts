/**
 * Bridge de compatibilidade para imports legados `@shared/schema`.
 *
 * Fonte canônica de schema: `@alice/shared/schema` (`packages/shared/src/schema.ts`).
 *
 * Author: Fillipe Guerra
 * Data: 2026-03-10
 */

export * from '@alice/shared/schema';

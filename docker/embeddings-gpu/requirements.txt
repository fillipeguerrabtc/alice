# Embeddings GPU Service - Dependências
# Alice Enterprise Platform
# Autor: Fillipe Guerra
# Data: 15 de Dezembro de 2025
#
# Serviço de embeddings multimodal via GPU (Salad Cloud)
# - Texto: BGE-M3 (1024 dim, multilíngue, 100+ idiomas)
# - Imagem: OpenCLIP ViT-H/14 (1024 dim)
#
# ARQUITETURA 100% GPU (Opção B - Alta Qualidade)
# Versões mais recentes de Dezembro 2025

# PyTorch 2.5.1 com CUDA 12.4 (versão estável compatível com Salad Cloud)
# Nota: PyTorch 2.7/2.8 requer CUDA 12.8/12.9 que pode não estar disponível em todas as GPUs Salad
torch==2.5.1
torchvision==0.20.1

# FastAPI 0.115.6 + Uvicorn (versões estáveis de dezembro 2025)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3

# Rate limiting
slowapi==0.1.9

# Métricas Prometheus
prometheus-client==0.21.1

# Circuit Breaker
pybreaker==1.4.1

# Embeddings de texto (BGE-M3 - 1024 dim multilíngue)
# FlagEmbedding 1.3.4 (dezembro 2025) - versão mais recente
# Suporta FlagAutoModel, multi-GPU, dynamic batch sizing
FlagEmbedding==1.3.4

# OpenCLIP para embeddings de imagem (ViT-H/14 - 1024 dim)
open_clip_torch==2.29.0

# Processamento de imagem
Pillow==11.0.0

# Numpy (versão compatível com torch 2.5.1)
numpy==2.1.3

# Transformers (dependência do FlagEmbedding)
transformers==4.47.0
sentence-transformers==3.3.1

# Hugging Face Hub
huggingface-hub==0.27.0

# Tokenizers
tokenizers==0.21.0
